# Tasks — one replay implementation

- [x] 1. `cli.ReplayResultFor` returns the comparison structurally.
- [x] 2. `Replay` renders it; pre-existing replay tests pass unchanged.
- [x] 3. The caveat travels in the result, on both outcomes.
- [x] 4. Tests + mutations: drop the caveat; report unavailable as divergence.
- [x] 5. Record the CONSOLE-10 blockers (`PLAT-5c`, the `decision.proto` question) on the roadmap.

# Decision Register

**This file is the single source of truth for D-numbers.** Every other document references
decisions *by number* rather than restating them. If a decision changes, it changes here first.

Why this exists: D1-D23 were previously scattered across six documents, each paraphrasing them
slightly differently. [`brief.md`](brief.md) drifted out of sync twice as a result — still describing a
Rust agent after D8 had made the project all-Go. Restatement is what rots; references don't.

Rationale lives in the research reports, which are **append-only historical records** and are
never rewritten: [`research-scouting-r1.md`](research-scouting-r1.md) (D1-D9) and
[`research-review-r1.md`](research-review-r1.md) (D10-D23, four adversarial reviewers).

Status: **firm** = settled, changing it is a project pivot · **open** = awaiting evidence or
an owner call · **scoped** = settled but with a named condition that could narrow it.

---

## Scope and phasing

| # | Decision | Status |
|---|---|---|
| **D1** | **Phase 1 is observe-and-audit only.** Decisions are recorded, not enforced. Enforcement lands in Phase 2 once the classifier's real false-positive rate is known. DLP has no cheap partial-credit MVP: false negatives leak, false positives block legitimate work. | firm |
| **D49** | **Enforcement is POST-DECISION: the engine records a Decision, then dispatches it to a registered enforcer that carries it out, auditing the outcome; observe-only is the default and enforcement CONTAINS, it does not PREVENT (Phase 2 kickoff).** The closed action set (D14) and a proven enforcer contract (T-020) existed but nothing carried out a Decision. Inline fanotify blocking (deny within the permission window) is deferred because the pipeline cannot complete in the 532µs budget (T-002) — classification reads whole files. So the tractable step is post-decision enforcement: `Engine.Enforcers`, invoked AFTER the Decision is recorded (order deliberate — the trail shows what was decided even if enforce fails or the process dies), with the enforcement outcome audited — a failed enforcement is a high-severity entry, never silence (D14). With NO enforcers registered the engine is observe-only (D1); registering one turns enforcement on per action. A real `quarantine` file enforcer moves a flagged file to an owner-only directory. The enforcement TARGET (the file path) is supplied by the engine from the event via a `TargetedEnforcer` interface — the Decision still carries only the verdict (D39's target gap avoided, not widened); the enforcer sees the Decision for the verdict and the target for what to act on, exactly as CrowdSec's decision carries the IP. **Honest bound (D16):** post-decision enforcement contains AFTER detection (the file was already read — that is how it was classified); it does not prevent the access that triggered it, and quarantine is defeatable by root. "Enforcement" is not "prevention". | firm |
| **D2** | **No clipboard events.** Wayland mediates the clipboard through the compositor and hands data only to the focused client — system-wide observation is *architecturally prevented*, not merely hard. Documented as a platform gap. | firm |
| **D7** | **Keep the core small.** Prior OSS DLP died of maintenance economics, not bad detection (MyDLP acquired, OpenDLP abandoned, Apache Metron retired to the Attic). Ambition is the failure mode. | firm |
| **D9** | **Windows/macOS via GitHub Actions runners, not local VMs.** Windows: a VM solves *dev* (test-signing loads unsigned drivers); shipping needs an EV cert (~$300-600/yr) + Microsoft attestation. macOS: Apple's EULA permits virtualisation only on Apple hardware, and Endpoint Security needs an entitlement Apple grants selectively. Not an infrastructure problem. | firm |

## Language and engine

| # | Decision | Status |
|---|---|---|
| **D8** | **Single language: all-Go** (and **pure** Go — T-005 confirmed `x/sys/unix` covers fanotify; event parsing is hand-rolled over a stable ABI, no CGO. Review finding A3 closed.), superseding the brief's Rust-agent/Go-backend split. One language ⇒ one shared policy engine ⇒ the cross-runtime parity risk is *deleted rather than solved*. Chosen over Rust because the owner does not write code: agent-written Go converges in fewer compile-fix cycles and is reviewable by a non-coding owner — the only real oversight mechanism here. | firm |
| **D19** | ~~D8 is scoped~~ — **RESOLVED 2026-07-20 by measurement, D8 stands unmodified.** Worst-case responder latency 532µs (typical 1-3µs) at 2 cores under 256 MB/s allocation and 10× realistic event rate. GC is not the risk; unbounded stalls are, and those are a design problem the fail-open watchdog addresses (D17/D18, T-011) — not a language problem. Evidence: [`spike-t002-gc-pause.md`](spike-t002-gc-pause.md). | firm |
| **D6** | *(collapsed)* Policy IR — dissolved by D8. OPA/Rego runs natively in Go; no CEL, no WASM, no custom IR. CEL was the trap: an official cross-language conformance suite exists but nobody runs it against the Rust implementation. WASM remains relevant only for sandboxing untrusted Hub packs (Phase 3+). | firm |

## Detection and enforcement

| # | Decision | Status |
|---|---|---|
| **D3** | **fanotify is the primary hook**, with **fail-open engineered from the first commit** (self-PID bypass, response-timeout watchdog, safe teardown). The kernel does not fail open for you — blocked processes park in `TASK_UNINTERRUPTIBLE` and can hang the machine. Blocks open/read/exec; **not** rename or unlink. | firm |
| **D4** | **Decision carries confidence, not certainty.** Classification is noisy by nature (Presidio ~22.7% precision on person names). Policy must consume confidence and thresholds, never a clean boolean. | firm |
| **D5** | **Endpoint classification is patterns and deny-lists only.** No spaCy/NER on the agent — Microsoft deploys Presidio server-side because the NER path is not endpoint-viable. | firm |
| **D14** | **The Enforcer action set is CLOSED and TYPED** (Block / Alert / Quarantine-local / Encrypt-local). An open action framework lets a compromised control plane express "upload file to URL", indistinguishable from normal telemetry. This is what makes "the server coordinates, does not control" architectural rather than aspirational. | firm |
| **D39** | **The USB enforcer acts on the global `authorized_default` posture; per-device enforcement is deferred (T-020).** D1 asked for one real, non-stub enforcer to prove the `Enforcer` contract end to end. `internal/enforcers/usb` implements `core.Enforcer`, advertises ALLOW/BLOCK, and enacts a Decision by writing `/sys/bus/usb/devices/usbN/authorized_default` (the standard Linux mechanism USBGuard builds on) — a real enforcement point. It refuses an action it does not advertise rather than silently no-oping (a no-op is an enforcement that did not happen but looks like it did). The producer (`internal/connectors/usb`) pseudonymises the USB serial at the source with a keyed HMAC — a raw serial is a durable re-identifier and must never enter the event stream (D23). **Honest boundary this surfaced:** the Decision carries `event_id` and the action but NO enforcement TARGET, so this is a GLOBAL posture, not "deauthorise THIS device". Per-device enforcement needs a typed target on the Decision — the small, identifiable core addition D26 predicts a new-shape capability requires — deferred until targeted enforcement is actually built rather than added speculatively. It does NOT test the fail-open/blocking contract (that is the fanotify watchdog's, T-011), and Phase-1 observe-only means nothing in the live pipeline invokes it — the enforcer is real and CAN block; a test drives it. | firm |
| **D34** | **The policy engine runs with a restricted capability set — no network, no clock, no randomness.** OPA/Rego (D6) is instantiated with every nondeterministic builtin and `opa.runtime` removed, filtered by OPA's own `Nondeterministic` flag so an OPA upgrade that adds a new clock or network builtin is excluded automatically. A policy that references `http.send`, `time.now_ns`, `rand.*` fails to LOAD, named, not at eval. This does three things with one mechanism: it makes "the server coordinates, it does not control" a capability boundary rather than a review promise — a distributed policy (Phase 2) cannot reach out regardless of its content; it makes decisions **deterministic and therefore replayable** (D27, `core.Replay`), the precondition for the audit trail being an investigation tool; and it removes `http.send`, an SSRF/exfil primitive, from every endpoint. The Decision's non-deterministic fields (`decision_id`, `decided_at`) are set by the Go layer, outside the policy. The action is mapped through a closed table (D14) — an action the enum does not define is a failed Decision, never a silent ALLOW — and confidence is clamped strictly below 1.0 (D4). OPA lives in `internal/policy`, never core (`check-core-deps.sh`). **Honest limit:** the capability set removes side effects, not CPU; a pathological policy can still be slow, which is a throughput concern because policy runs off the permission-response path in observe-only Phase 1, not a machine-hang. | firm |
| **D17** | **Fail-open is per-policy, and is itself a bypass.** Make classification slow (huge file, zip bomb, ReDoS, load) and every Block becomes an Allow — the starvation trick used against WAFs. Every timeout-allow emits a high-severity audit event; scan budgets capped; timeout *rate* is its own signal. | firm |
| **D43** | **Every terminal pipeline outcome is logged structurally (stdlib `log/slog`) with the event id as correlation id and a stable error CATEGORY (T-028).** Full OTel tracing is cut from Phase 1, but silent-in-the-logs is the milder cousin of silent-in-the-audit and the same rule applies (D17): a failure must be loud and countable. The dispatcher logs decided/failed/timeout/no-decision/not-recorded with `event_id`, `stage`, `kind`, `severity` and `category`, at a slog level matching the severity (a timeout logs at Warn — it turns a Block into an Allow, so a rising rate is greppable). `core.Category(err)` maps errors to stable slugs by IDENTITY (`errors.Is`), not by string, so wrapping does not change a category and a consumer can alert on `category=not_recorded` without parsing prose. **Logs carry ids and categories ONLY, never content** — a log is a wire (D10), asserted by a test that a content marker never appears. The logger is nil-safe (discard default); the wiring point is `Dispatcher.Logger`, set by whatever assembles the pipeline. Correlation id is the event id, not a trace/span id — OTel remains deferred. | firm |
| **D18** | **Phase 1 exercises the fail-open watchdog for real** (injected delay, assert auto-`FAN_ALLOW`), even though verdicts stay always-allow. USB enforcement does *not* test this contract — attach-time allow/deny has no blocked process and no race. **BUILT (T-011):** `internal/agent/watchdog` races evaluation against a hard per-event budget and answers the kernel with `FAN_ALLOW` on timeout, emitting a HIGH-severity audit event; self-PID is allowed by identity before any evaluation (no self-deadlock); the answer precedes the ledger write, and an audit failure surfaces without retracting the allow. The kernel-facing `fanotify_response` write is a thin `Responder` adapter, so the decision logic is fully tested without CAP_SYS_ADMIN; a privileged integration test exercises the real `FAN_OPEN_PERM` path and skips LOUDLY otherwise. Separate mechanism from the dispatcher's `StageDeadline` (which bounds an in-process wait Go cannot preempt) — the watchdog owns the kernel answer itself. | firm |
| **D16** | **Tamper-detection, never tamper-prevention.** Heartbeat / dead-man's-switch / "agent last seen". Anyone with root defeats a host agent; there is no fix that doesn't require distrusting the OS the agent runs on. | firm |
| **D42** | **The dead-man's-switch reports a silent host as OVERDUE, not tampered — a signal for a human, not an accusation (T-018).** This backs D16's honest claim: tamper-DETECTION is only real if the ABSENCE of an agent is detectable. The agent emits a `Heartbeat` (agent_id, sequence, observed_at) on its own subject; the control plane records it so "last seen per host" is a queryable fact that advances whether the agent reported real telemetry or only checked in. `OverdueAgents` is a PURE detector over last-seen times and a threshold — an agent silent past the threshold is overdue — kept pure because the logic that decides "someone should look" must be trivially verifiable. A clean `systemctl stop` is itself recorded (an `ExecStopPost` hook), so the case that STANDS OUT is "silent past threshold with no stop record", not an ordinary operator stop. **Honest limits (D16, D41):** defeatable by root (stop the agent, suppress the heartbeat, mask the unit, block the control plane) and by a compromised control plane (forge a heartbeat); last-seen lives in the non-evidentiary aggregate. Overdue ≠ tampered — legitimate offline periods exist and the offline queue (T-024) heals brief gaps; the value is noticing the COMMON case, not defeating an adversary who owns both ends. The threshold is several heartbeat intervals so normal jitter does not cry wolf. | firm |
| **D33** | **The endpoint detector engine MUST be a linear-time (RE2-class) matcher.** Classification runs on attacker-influenced bytes in the unprivileged worker, so a backtracking regex engine is a denial-of-service primitive — and because slow classification fails open (D17), a pattern driven to catastrophic backtracking turns every Block into an Allow. Go's `regexp` (RE2) cannot be driven to super-linear time, which removes the primitive rather than mitigating it. This is why the classifier (T-007) uses stdlib `regexp` and why introducing a backtracking engine for detection is forbidden, not discouraged. Detectors are format-match plus a validator (CPF/CPF-check-digits, card/Luhn, SSN/structural-only) emitting **type + confidence + count** with confidence never 1.0 (D4) and no content, hash or fingerprint on the wire (D10/D11) — asserted by a wire-byte grep, not prose. | firm |

## Privacy and data handling

| # | Decision | Status |
|---|---|---|
| **D10** | **No exact-match hashing of low-entropy PII.** SSN and Brazilian CPF ~10⁹, cards ~10⁷ after BIN+Luhn, DOB ~36.5k — all brute-forceable, and **salting does not fix it**. Worse, cross-endpoint matching needs a deterministic key, which would have to live on every agent. Use format+checksum detection locally; transmit **type + confidence + count** only. Keyed EDM for high-entropy composite records, key server-side only. | firm |
| **D11** | **Embeddings and fuzzy fingerprints are content-equivalent.** Similarity-preserving hashes leak structure by construction; vec2text recovers ~92% of 32-token inputs exactly. Encrypted, access-controlled, **never distributed through the Hub**. Not a de-identification technique. | firm |
| **D12** | **Audit ledger = Postgres hash chain + forward integrity + external anchoring.** JetStream is a **bus only** — bounded retention is not a system of record. Key-evolving schemes so a post-compromise attacker cannot rewrite pre-compromise entries. Claim **"tamper-evident with forward-integrity between anchors"**; never "tamper-proof", which is unachievable in a single self-hosted trust domain. | firm |
| **D38** | **Completeness is provable only between witnessed anchors (T-019).** The hash chain and forward-secure signatures prove history was not ALTERED; they cannot prove it was not TRUNCATED — a root attacker can destroy the chain and rebuild a shorter, internally-consistent one. An `Anchor` is a witness-signed `(sequence, hash)` checkpoint of the ledger head; because the chain is linear, the head hash commits to the whole prefix, so no Merkle inclusion proof is needed. The `Witness` key MUST live in a trust domain the deployer does not control (a second host, WORM storage, a public transparency log) — an anchor the agent could forge proves nothing, since the agent is the party that might rewrite the log. `VerifyChain` matches each valid anchor to the chain, fails if a witnessed checkpoint is no longer satisfied (truncation of witnessed history is now DETECTED), and reports `AnchoredThrough` — completeness is ANCHORED only when a witness covers the last entry, else the prefix is proven and the tail stays UNVERIFIED. A forged anchor (wrong witness key) is ignored, never able to fail an honest chain. **Honest limits, stated where the interval is configured:** the undetectable-loss window equals the anchor interval (everything since the last anchor can still be truncated), and an anchor is only as trustworthy as the independence of its witness — Phase 1's witness is a separate keypair that proves the mechanism, not real third-party trust. | firm |
| **D20** | **Privacy-law features are Phase-1 architecture, not later additions:** enforced retention with automatic purge, purpose tagging at schema level, exclusion lists as a first-class policy primitive, pseudonymisation by default, audit trail of who *viewed* an investigation, four-eyes before HR-visible outcomes, a shipped DPIA template. GDPR Art. 35 makes a DPIA effectively mandatory; German works councils hold an **absolute veto** (BetrVG §87(1)(6)). | firm |
| **D47** | **View accountability is recorded by the write-capable control plane when it serves an investigation (T-013 seam, D20).** T-013 built `Ledger.RecordView` but the query CLI is a signer-less verifier that cannot append (D30); the write surface that serves fleet queries is the control plane (D41). `Server.View` serves an investigation's telemetry AND records the view — viewer, event, time — into `investigation_views` in one call, so obtaining the evidence and leaving a record are inseparable (the view is recorded FIRST: an attempted view is worth recording). An empty viewer is rejected — no unattributable view is silently kept — and callers prefix `unauthenticated:<os-user>` because there is still no authenticated OPERATOR identity (a sibling gap to agent identity T-017/D44; agent identity authenticates AGENTS, not the humans querying). `Views`/`ViewsBy` read it back — the "who looked at this" D20 requires. **Honest limits:** the view log is NOT the evidentiary ledger — no hash chain, no signatures, a compromised control plane could alter or omit it (same caveat as the aggregate, D41) — and the viewer is self-asserted (an OS identity), not verified. Loud-but-weak accountability beats an absent trail; overclaiming it as tamper-proof is forbidden. | firm |
| **D36** | **Retention purge tombstones rather than deletes (T-013), so GDPR erasure and the hash chain coexist.** Enforced retention (Art. 5/17) requires erasing expired personal data; the ledger is hash-chained, so deleting a row breaks every row after it. A purge instead erases the content columns (subject, decision payload, purpose) while KEEPING the skeleton — sequence, prev_hash, hash, sig, key_epoch. `VerifyChain` treats a tombstoned entry as an authenticated link: it still checks the prev-hash link and the signature over the stored hash, and ONLY waives the "hash matches current content" recompute, because the content is deliberately gone. `VerifyResult.Tombstoned` counts erased entries so an auditor sees erasure openly. Retention classes map to ages (short 30d / standard 365d / investigation HELD — a legal hold overrides routine purge; unspecified is treated as bounded, never an indefinite-retention hole). **Residual, stated:** a root attacker can tombstone+erase a LIVE entry to hide it, indistinguishable from a lawful purge without external anchoring — the SAME limit truncation already has (T-019), covered by the existing completeness caveat. **View accountability** (D20) is a `Ledger.RecordView` mechanism, built and tested; wiring it behind the query CLI is deferred to the write-capable control plane (T-023), because appending needs the signing key and the CLI is a pure verifier that must hold no signer (D30) — a discovery from implementation, not a dropped task. The four-eyes gate and employee-notice mechanism concern enforcement and are deferred to that phase; a DPIA template ships now (`docs/dpia-template.md`). | firm |
| **D23** | **UEBA supports both modes, gated.** Self-baseline computed locally, on by default. Peer-baseline is an **optional server-side Analytics module, off by default**, with its own consent/DPIA gate. Resolves the brief's silent contradiction between "Peer Analysis" and "the server coordinates, does not compute". Phase-1 cost is one field: a stable pseudonymous user ID. | firm |

## Runtime architecture

| # | Decision | Status |
|---|---|---|
| **D24** | **The endpoint pipeline is in-process and synchronous; NATS is the agent↔control-plane boundary only.** The brief names NATS as "the message bus" and says components communicate exclusively through events — read literally on the endpoint, that puts a broker inside the fanotify permission window, where [T-002](spike-t002-gc-pause.md) measured a 1-3µs typical / 532µs worst-case budget with a real process blocked in `TASK_UNINTERRUPTIBLE`. A broker round trip does not fit, and local-first evaluation (D1) means it should not have to. Both mechanisms are "the event bus" in the brief's language; only the second is NATS. Enforced by CI: `internal/core` may not import a broker (`scripts/check-core-deps.sh`). | firm |
| **D41** | **The control-plane store is the fleet AGGREGATE view, distinct from and NOT carrying the agent ledger's evidentiary guarantees (T-023).** The server side was referenced everywhere (T-017, T-018, verification) and built nowhere. `internal/controlplane.Server` subscribes to the three telemetry subjects the transport publishes (`openshield.v1.events/classifications/decisions`) and persists what it receives to `fleet_telemetry`. It stores only boundary-safe telemetry BY CONSTRUCTION: the transport has no `LocalClassification` method (D10), so only the `ClassificationSummary` — type+confidence+count — can arrive, and there is no path by which content could. A malformed message is COUNTED (`DecodeFailures`), never a silent vanish and never a stalled subscription. **The aggregate is NOT the evidentiary ledger:** it has no hash chain and no forward-secure signatures, a compromised control plane could alter it, and the evidentiary record is the agent's local forward-secure ledger (D12/D30), externally anchored (T-019). `agent_id` is self-asserted until identity (T-017). It coordinates and observes — it does NOT distribute policy or control agents (D14, "the server coordinates, it does not control"); Phase-1 policy is a local file (D1). Proven end to end over an embedded NATS + real Postgres. | firm |
| **D50** | **Telemetry is verified on ingest against the enrolled agent key — bad signature / unknown / revoked / replay is rejected and counted, a sequence gap is recorded — so the fleet view becomes attributable and suppression detectable (T-017 over the wire).** Per-agent identity (D44) existed but was never applied to the stream, so the aggregate was self-asserted (D41). Now agents sign each telemetry message in a `SignedTelemetry` envelope (agent_id, monotonic sequence across all kinds, payload, signature over `canonical(agent_id, seq, payload)`) via a `SignedPublisher`; the control plane subscribes to `openshield.v1.signed`, calls `VerifySigned` (signature vs the ENROLLED key, sequence for gaps/replays), and only on success persists — attributed to the VERIFIED agent, marked `verified=true`. An unverifiable message is REJECTED and counted (`RejectedTelemetry`), never stored; a gap increments `Gaps` while the authentic message is still stored. The legacy unsigned path remains for a not-yet-enrolled dev agent and is stored `verified=false`, so an aggregate query can tell attributable from self-asserted — the unsigned path is never silently trusted as verified. **Honest limits:** this authenticates the MESSAGE, not the connection (mTLS is complementary, a TLS-config concern); and root on the agent host forges (D16) — the guarantee is attributable-and-revocable telemetry with detectable suppression, not unforgeable-against-host-root. | firm |
| **D51** | **Agents enrol over the network via `POST /enroll` with a single-use token; token ISSUANCE is NOT a network endpoint (T-017 over the wire).** `Enroll` was in-process only, so onboarding could not happen over the wire and the signed-telemetry chain (D50) had no way to register an agent. The control plane now serves `POST /enroll {token, agent_id, public_key(base64)}` → the existing `Enroll` (token verified/unused/unexpired → identity recorded, token burned). Token issuance is deliberately NOT exposed — an unauthenticated mint endpoint would defeat the single-use model; issuance is an operator-local action, tokens handed to agents out of band. Enrollment refusals are GENERIC (401 "enrollment refused"), and the three token states (unknown/expired/used) are indistinguishable — guaranteed structurally by `Enroll` collapsing them into one `ErrEnrollment` sentinel — so a prober learns nothing about the token space; a malformed body or wrong-size key is a 400. **Honest limit:** the endpoint is HTTP — the single-use short-TTL token limits interception value, but production MUST front it with TLS (a config step, not logic; the same transport-auth concern T-017 named). Proven with httptest + real Postgres: a valid token enrols and the agent's signed telemetry then verifies. | firm |
| **D44** | **Each agent has its own Ed25519 identity (never a shared secret), enrolled via a single-use short-TTL token, signing telemetry with a monotonic sequence the control plane verifies for gaps; identity is revocable per agent (T-017).** The control plane previously accepted a self-asserted agent_id (D41); review (A6) flagged a shared fleet secret as fleet-wide risk — one compromised agent equalling fleet compromise. Now: the agent generates its own keypair (private key never leaves the host); an admin issues a token stored only as a SHA-256 hash with an expiry; `Enroll` verifies it is present/unexpired/unused, records the identity, and BURNS the token in one transaction (single-use). A token is enrollment-only, not a signing key — a stolen token enrolls a bogus (revocable, visible) agent but cannot impersonate an existing one. Telemetry is signed over a length-prefixed envelope with a monotonic sequence; `VerifySigned` rejects an unknown/revoked agent and a bad signature, ACCEPTS-and-records a sequence GAP (suppression between agent and control plane — the audit log's evidentiary reasoning: a trail that cannot reveal suppression is not evidentiary), and REJECTS a replay/reorder (seq ≤ last). `Revoke` is per-agent, so containing one endpoint does not touch the fleet. **Honest limits (D16):** root on the host reads the private key and can sign anything the agent could — the guarantee is attributable-and-revocable, not unforgeable; a gap is a signal (lossy network vs suppression), not an accusation; the key is in-memory (disk persistence for restart is deployment); and mTLS (authenticating the CONNECTION) is a complementary transport-layer concern, not this (which is the evidentiary application layer). | firm |
| **D25** | **Per-stage deadlines are owned by the dispatcher, not the stage.** A stage that sets its own deadline can set it to infinity, and an unbounded stage is the mechanism by which the responder hangs a machine. **Honest limit:** Go cannot preempt an uncooperative function, so the deadline bounds the dispatcher's *wait* and a pathological stage's goroutine may outlive it. That is why T-011's fail-open watchdog — which answers the kernel regardless of what the pipeline is doing — is an independent mechanism rather than a consequence of this one. | firm |

| **D26** | **The zero-core-change claim is narrowed, and the CI fitness test is necessary but not sufficient.** Capabilities of the same *shape* as existing ones need no core change; a new *shape* of data flow needs a small one. Demonstrated by [T-004](design-t004-peer-ueba.md), which also produced a worked example of the fitness test being gameable: letting Policy query the analytics store directly passes CI with zero core diffs while destroying the stage isolation the test exists to protect. Green CI is not evidence the architecture held. **BUILT (T-014):** `internal/fitness` adds a connector+stage+enforcer from outside core and flows an event to a Decision (proof a capability needs no core edit); `scripts/check-capability-boundary.sh` fails the build if core imports any capability package; and `TestFitnessTestKnowsItsLimits` guards this very caveat against silent deletion. Still necessary-not-sufficient. | firm |
| **D53** | **Peer-UEBA was built, and it confirms D26 IN CODE: a genuinely new-shape capability needed exactly ONE small, identifiable core change — no more, and the stronger zero-core-change claim is FALSE.** T-004 designed peer-baseline UEBA on paper; this built it. Peer-UEBA is the new shape precisely because it is STATEFUL (accumulates behaviour) and CROSS-ENTITY (a subject is risky RELATIVE TO ITS PEERS, not by a per-event rule), which breaks every assumption a per-event DLP pipeline makes. Building it surfaced the one core gap, found before writing the capability: the `Dispatcher` built `State{Event: e}` with `Context` always nil and NO injection point, and the policy hardcoded `context: nil`. The single core change is `Dispatcher.ResolveContext func(*Event) *Context` — one field, one guarded call, nil-default (behaviour unchanged). Everything else is OUTSIDE core: `internal/analytics/peerueba` accumulates per-subject activity and computes a peer z-score → risk [0,1] + a `context_version` (D27), exposed to the policy as a closed typed set (D28), and a policy rule escalates on high peer risk with NO PII hit — proven end to end (analyzer → resolver → the hook → policy → ALERT). The capability-boundary check now bans core from importing `analytics`/`engine` too. **The honest verdict:** the architecture absorbs a new-shape capability with a small NAMED addition — validating D26's narrow claim — and the brief's implied zero-core-change version is disproven. Pretending none was needed would be the overclaim the fitness test exists to expose. Risk model is a toy z-score (proves the shape, not detection quality); peer-UEBA is server-side, off by default, with a consent/DPIA gate (D23). | firm |
| **D54** | **peer-UEBA's real home is the CONTROL PLANE, not the endpoint — it now runs server-side over the verified fleet stream and produces investigations, never controlling agents.** peer-UEBA is stateful and CROSS-ENTITY: a subject is risky only RELATIVE TO ITS PEERS, and a single endpoint has no peers. So the capability cannot function at the endpoint at all; it can only run where the whole fleet's activity converges — the control plane. This resolves the two seams D53 exposed: the endpoint `Dispatcher.ResolveContext` hook is the CORE seam a new-shape capability needs (proven in D53), but the RUNNING system integrates peer-UEBA as a server-side analytics consumer of the verified telemetry (D50). On each VERIFIED `event` the control plane feeds the pseudonymous subject (D23) to the analyzer and, when its peer-relative risk crosses an operator-set threshold, records a **peer alert** — a server-side DERIVATION stored in `peer_alerts`, deliberately APART from received telemetry (a derivation must not masquerade as agent-attested evidence; it is not the ledger either, D38) and throttled per subject by a rising-edge cooldown. Three boundaries hold: only verified telemetry moves a baseline (an unverified message is not evidence, D50); it is OFF unless explicitly enabled (the D23 consent/DPIA decision, not a default); and it OBSERVES — no risk is fed back to agents and no agent behaviour changes (D14). The endpoint resolver seam remains valid but unused by the fleet path; the two are distinct and the server one closes no control loop. Proven live: the fleet e2e drives an outlier that is flagged exactly once while a typical subject is not. The z-score risk model is still a toy that proves the SHAPE, not detection quality. | firm |
| **D55** | **Mutual TLS protects the agent-facing channels as a layer BENEATH Ed25519 message signing — the channel is authenticated and confidential, the payload is still independently signed, and both are enforced.** Signing (D50) is a MESSAGE guarantee (integrity + attribution) over what was a PLAINTEXT channel: telemetry was readable on the wire and an enrollment token could be captured in flight and replayed. mTLS closes that: the enrollment HTTP endpoint (which the app owns) is served with `RequireAndVerifyClientCert` — the app itself demands and verifies the client cert. The NATS legs are TLS 1.3 with each side presenting a client cert and verifying the other against the configured CA; but on the telemetry leg the requirement that the AGENT present a valid client cert is enforced by the NATS BROKER's `--tlsverify`, NOT by this code — `tlsconf.ClientConfig()` only supplies a client cert and a CA to trust, it cannot make the broker demand one. So a deployment MUST start NATS with `--tlsverify --tlscacert` (the reference `deploy/mtls-e2e.sh` does); a broker without it would accept a certless TLS client on the telemetry leg even while the server logs \"mutual TLS enabled\". The blast radius is bounded — D50 signature verification still rejects forged telemetry, so evidence integrity survives a mis-set broker; only the channel-auth defence-in-depth layer is lost. It is DEFENCE IN DEPTH, not a replacement — a cert-authenticated peer whose telemetry fails signature verification is STILL rejected (proven by test), and a signed message cannot arrive over a failed handshake; neither layer does the other's job. It degrades HONESTLY: TLS is off by default (the dev loop and existing tests stay plaintext), enabled only by explicit CA+cert config (`OPENSHIELD_TLS_CA/CERT/KEY`), and a partial/unreadable config fails LOUD at startup — there is no try-TLS-then-plaintext downgrade, because a silent downgrade would reopen the gap. One loader (`internal/transport/tlsconf`) feeds all three call sites. Certificate issuance/rotation/PKI is out of scope (the e2e uses a throwaway CA); host root still defeats the at-rest key (D16, the signer-key bar); the control plane still only observes (D14). Proven live in `deploy/mtls-e2e.sh`: a cert-bearing agent enrolls over HTTPS and publishes verified telemetry over TLS NATS, while a no-cert agent is refused with zero rows. | firm |
| **D56** | **The investigation view-audit viewer is now bound to a VERIFIED mutual-TLS client certificate (`operator:<CN>`), closing the self-asserted-identity gap the code itself flagged.** T-013 requires auditing WHO viewed an investigation, but `RecordView`/`View` took a caller-supplied `viewer` string — the honest placeholder was `unauthenticated:<os-user>`, a name nothing verified. Reusing the mTLS layer (D55), the control plane exposes an authenticated `/view` endpoint, mounted ONLY under mutual TLS, whose recorded viewer is derived from the verified client certificate's subject, never from the request. A request with no verified certificate is refused with no view recorded (the D20 no-unattributable-view rule, now enforced by authentication rather than a non-empty check). Authenticated views read `operator:<CN>`; the legacy library path keeps its explicit `unauthenticated:<os-user>` marking, so the trail distinguishes accountable views from self-asserted ones. SCOPE, stated honestly: this is AUTHENTICATION, not AUTHORIZATION — any CA-issued cert authenticates as an operator (enrollment and view share a CA today), and telling operator-role certs from agent-role certs (separate CA or cert OU/roles) is a documented follow-up. The viewer is now cryptographically bound to a held credential — a strict improvement over a free string. Host root still defeats the key (D16); the control plane still only observes (D14). Guards mutation-tested. | firm |
| **D57** | **encrypt-local is the second real enforcer — it carries out ENCRYPT_LOCAL by making a flagged file genuinely unreadable in place, proving the D49 dispatch seam absorbs a cryptographic primitive, not just relocation.** Quarantine MOVES a file; usb allows/denies at attach. Encrypt-local (`internal/enforcers/encryptlocal`) replaces the file's CONTENTS with AES-256-GCM (fresh per-file nonce, `magic || nonce || ciphertext`), written atomically (temp+rename) so a crash leaves the original or the whole ciphertext, never a partial, and idempotent via the magic header so a re-encrypt does not double-encrypt. An exported `Decrypt` supports operator recovery and lets the guard PROVE unreadability: on-disk bytes differ from plaintext, a wrong key fails the GCM tag, and the right key recovers the exact original — a rename dressed as encryption would fail these. It plugs into the engine's existing `Enforcers` slice with NO change to the Decision contract; proven end to end (policy decides ENCRYPT_LOCAL → file encrypted on disk → outcome audited, decision first, D14). HONEST SCOPE: containment after detection, not prevention (the file was already read to be classified, D49); and its value depends entirely on KEY CUSTODY — an on-host key readable by the agent user or host root defeats it (D16, the signer-key bar), so the real protection is against a stolen disk or a different local user. Key management/escrow/rotation is out of scope (loaded from a file). Guards mutation-tested. | firm |
| **D58** | **Cert-role authorization closes the D56 hole: a verified mutual-TLS client certificate is now authorized PER ROUTE by its role (Subject OU `agent`/`operator`), so an agent cert can no longer read investigations and an operator cert can no longer masquerade as an agent onboarding.** D56 gave authenticated operator identity but `/enroll` and `/view` shared one TLS config trusting one CA — so any CA-issued cert (including an agent's enrollment cert) could call `/view`: authentication without authorization. Now a `requireRole(role, handler)` gate reads the role from the VERIFIED peer certificate's OU and enforces it per route: `/view` requires `operator`, `/enroll` requires `agent`. A verified cert of the wrong (or unrecognised) role is refused `403 Forbidden` — distinct from the `401` for NO verified cert, so the trail separates "nobody" from "somebody not allowed here". Deny-by-default: a cert with no recognised role is authorized for nothing. The plaintext dev loop is unchanged (no cert, no role; `/view` still absent, D56). SCOPE, honest: this is authorization by a certificate ATTRIBUTE the issuing CA sets — only as trustworthy as the CA's issuance discipline (a CA that signs `OU=operator` for the wrong party loses, the PKI trust class); the win is the role is CHECKED. A production system might prefer a dedicated policy OID over OU (a refinement, noted). D14/D16 hold. Proven live in `deploy/mtls-e2e.sh`: operator `/enroll`=403, agent `/view`=403, operator `/view`=200. Guards mutation-tested. | firm |
| **D59** | **Encrypt-local key ESCROW closes the D57 custody gap: in escrow mode the endpoint holds only a recipient PUBLIC key and CANNOT decrypt what it seals — recovery requires the PRIVATE key held off the endpoint.** D57's symmetric encrypt-local was honestly limited: an on-host key readable by the agent user or host root defeats it, so a fully-compromised endpoint holds both ciphertext and key. Escrow mode fixes WHERE the trust sits: using Curve25519 anonymous sealed-box (`nacl/box`), the enforcer seals each flagged file to a provisioned recipient public key; an attacker who fully compromises the endpoint gets the ciphertext, the sealed blob, and the public key — none of which decrypts the file. Only the off-endpoint private key does, via `DecryptEscrow`. It is a MODE of the existing enforcer (`NewEscrow`/`WithEscrowKey`), not a rewrite: symmetric mode (D57) stays, escrow blobs carry a DISTINCT magic (`OSENCX1` vs `OSENC1`) so blobs are self-describing and the modes never silently cross (guarded doubly, by magic AND AEAD). All D57 invariants hold (atomic temp+rename, idempotent, empty-target errors, containment-not-prevention). Proven: an escrow blob does not open with the public key/endpoint material and only the private key recovers the exact original; and end to end, a policy deciding ENCRYPT_LOCAL in escrow mode seals a file only the escrow key recovers, audited. HONEST RESIDUAL: escrow shifts trust to the PRIVATE-key holder — it defends against ENDPOINT compromise, not against compromise of the escrow holder; and escrow-key distribution/rotation and the private key's own at-rest custody (D16) remain operational concerns. D14 holds. Guards mutation-tested. | firm |
| **D60** | **Minimal provisioning tooling makes the mTLS / cert-role / escrow stack (D55/D58/D59) actually deployable — it issues the credentials that were previously assumed to exist out of band.** Everything built for access security depended on certificates and keys nothing issued; they lived only as hand-rolled `openssl` in the e2e. `internal/provision` + `cmd/openshield-provision` now issue them: `ca-init` (Ed25519 self-signed CA), `cert --role agent|operator` (a leaf signed by the CA carrying the role in Subject OU per D58, serverAuth+clientAuth, SANs), and `escrow-keygen` (a Curve25519 keypair — public key to endpoints for `NewEscrow`, private key to the off-endpoint vault for `DecryptEscrow`). The provisioning tool is a SEPARATE authority binary, deliberately NOT part of the read-only `openshieldctl` (which holds no signer) — minting credentials mirrors the server's admin-local `issue-token`. The loop is proven closed: issued leaves verify against the CA by x509, carry the right OU, load via `tlsconf.Load`, and — the decisive test — a PROVISIONED operator cert authorizes for `/view` while a provisioned agent cert is 403, driving the REAL D58 gate; a provisioned escrow keypair round-trips through the enforcer. A drift guard asserts the tool's role strings equal `controlplane.RoleAgent`/`RoleOperator`. `deploy/mtls-e2e.sh` now generates its CA and certs WITH the tool (replacing raw openssl), so the live container run proves the tool's output works with the server, the role gate and the TLS NATS broker. SCOPE, honest: MINIMAL provisioning for dev/small fleets, NOT a full PKI — no revocation/CRL/OCSP, no rotation automation, no HSM. The CA private key and escrow private key are the trust roots (D16): whoever holds `ca-key.pem` can mint any cert including an operator cert, and whoever holds the escrow private key reads every escrowed file — documented, keys written 0600, production should front issuance with a real CA/vault. D14 holds. Guards mutation-tested. | firm |
| **D61** | **Detection quality is now MEASURED (classifier) and the peer-UEBA baseline is HARDENED — closing the two detection-quality gaps the roadmap and the D54 audit named.** (A) The classifier had no measured FP/recall and its whole FP defense — the checksums — was untested against adversarial near-misses. A labeled synthetic corpus (generated valid CPFs/Luhn cards, near-misses with a flipped check digit, SSN-shaped numbers, committed clean prose/code/log fixtures) now measures it: CPF and card recall 1.000 with ZERO false positives on near-misses (a failed checksum never flags), while the checksum-free SSN detector flags 1.000 of SSN-shaped numbers — the MEASURED reason its confidence is capped low (D4/D5), no longer just a comment. The numbers are a regression guard: weakening a checksum makes near-miss FP jump. (B) peer-UEBA's toy z-score had two real flaws: SELF-CONTAMINATION (a subject sat in the mean/std it was judged against) — fixed with a LEAVE-ONE-OUT baseline (an outlier now scores strictly higher than when self-included); and NO DECAY (a past burst stayed flagged forever) — fixed with deterministic time decay against an injected clock (a stale burst fades out of anomaly, 0.000, where a cumulative count keeps it at 1.000). Hardening surfaced a further real flaw — near-uniform peers + decay noise made the z-score explode (divide by ~0 std) — fixed with a coefficient-of-variation std floor, so a subject must be MEANINGFULLY above uniform peers to score. The public Analyzer API (Observe/ContextFor/Resolver) and context_version (D53) are unchanged; it remains a statistical baseline, off by default, observe-only (D54), NOT a trained detector. HONEST BOUND: the corpus is SYNTHETIC — it validates the checksum FP defense and discrimination, it does NOT establish real-world base rates, which still require live dogfood (T-015). Guards mutation-tested. | firm |
| **D62** | **The observe pipeline is assembled into the RUNNING `openshield-engine` binary — closing the audit finding that the product did not run, only its components (driven by test code) did.** `cmd/openshield-engine` built the whole pipeline (signer, ledger, policy, worker, engine) then discarded it (`_ = eng`) and idled; `cmd/openshield-agent` printed "not implemented". So every "runs end to end" claim was proven by integration tests calling internal packages, never by a shipped binary — the sharpest instance of the project's own "verifies against its own assumptions" failure. Fix: the engine now opens the fanotify connector ITSELF in unprivileged NOTIFY mode (D52 — observe needs no privilege), watches `OPENSHIELD_WATCH_DIRS`, and runs each event through classify→policy→decide→audit; watch config is validated first (an engine watching nothing fails loud, not a silent no-op, D17-spirit). Proven at the BINARY level (`deploy/observe-e2e.sh` + a no-DB refusal test): the real engine+worker binaries, a file dropped in a watched dir, an ALERT in the forward-secure ledger — exercising process boundaries, env config, the worker subprocess and the real ledger, which package tests cannot. The observe path needs NO privileged agent; the privileged permission-mode agent (`openshield-agent`, now honestly a deferred-component stub that exits non-zero) is for INLINE blocking only, still deferred (D49 — classification cannot fit the 532µs permission-window budget, so OpenShield contains after detection, it does not prevent inline). README/CHANGELOG corrected to say the observe path runs as the binary and inline blocking is deferred, with no wording implying the full privileged path ships. Guards mutation-tested. | firm |
| **D63** | **The audit ledger is now APPEND-ONLY at the database level — closing the audit finding that a leaked connection string, not host root, was enough to rewrite history.** Migration 001 left `audit_entries` fully mutable (no RLS, no REVOKE, no trigger), so tamper-EVIDENCE was purely a read-time `Verify()` property: anyone with the DSN (a leaked `.env`, a backup, a misconfigured network) could rewrite or truncate the hash chain. Migration 010 installs a `BEFORE UPDATE OR DELETE` trigger that forbids DELETE, forbids UPDATE of any INTEGRITY SKELETON column (`sequence, appended_at, prev_hash, hash, sig, key_epoch, retention_class, context_version`), and forbids resurrecting a tombstoned entry — while PERMITTING the D36 retention tombstone (content-only UPDATE + `tombstoned_at`). A positive test proves the app role can no longer DELETE a row or change a hash; the existing tamper tests, routed through an owner-privilege trigger-disable that faithfully models an adversary who got PAST the DB control, still prove `Verify()` catches the bypasser — the two layers tested independently. HONEST RESIDUAL: a table OWNER can `DISABLE TRIGGER`, so this stops the SQL-level / backup-restore / leaked-DSN attacker, NOT a determined one who bypasses it; the COMPLETE fix is running the ledger under a NON-OWNER restricted role (a documented follow-up), and the guarantee against a bypasser remains `Verify()` + external anchoring (D38 — its operationalization is the next step). Raises the bar from "leaked connection string = rewrite history" to "leaked connection string cannot tamper via SQL." Guards mutation-tested. | firm |
| **D64** | **External anchoring is now OPERATIONAL — closing the audit finding that `AnchorHead` (T-019/D38) was implemented and tested but had ZERO callers, leaving every deployment permanently `Completeness: UNVERIFIED`.** Anchoring is the ONLY control that catches an adversary who bypasses the D63 append-only trigger (a table owner can disable it) or truncates witnessed history — and it never ran. Four pieces make it runnable: (1) witness-key PERSISTENCE — `core.NewWitness` only generated a keypair; `WitnessFromKey`/`PrivateKey()` let a witness be saved once and reloaded, so anchors are signed under a STABLE key a verifier can check. (2) `cmd/openshield-anchor` — a witness authority tool (separate from the read-only openshieldctl) that loads the witness key, opens the ledger SIGNER-LESS (`OpenForVerify` — it attests to the head, it cannot append), calls `AnchorHead`, and logs the sequence; one-shot for a systemd timer, `--interval` for a loop. (3) `openshield-provision witness-keygen` mints the witness keypair. (4) `openshieldctl verify --witness` sets the witness public key so an auditor can confirm `CompletenessAnchored` (read-only). Proven it MOVES completeness: an integration test (signer-less anchor via a saved-key witness → `Verify` returns Anchored, and UNVERIFIED without the witness pub) and BINARY-level in `deploy/observe-e2e.sh` (completeness before=unverified → after=anchored via the real binaries). HONEST TRUST MODEL (T-019, in the tool's own output + a systemd unit comment): the witness MUST run in a trust domain the deployer does NOT control — a key the deployer holds attests to little; the tool makes anchoring RUNNABLE and schedulable, custody is the operator's and DETERMINES the guarantee, and the undetectable-loss window is the interval between anchors (the schedule chooses the window). install.sh now also builds openshield-engine/anchor/provision (advancing packaging). Guards mutation-tested. | firm |
| **D65** | **The file enforcers no longer FOLLOW a symlink at the flagged path — closing the classification→enforcement TOCTOU that let a non-root local user turn a defensive action into an arbitrary-file-read.** `encryptlocal.EnforceTarget` did `os.ReadFile(target)` (follows a symlink → reads a file the agent should never read) and quarantine's cross-fs fallback did the same; an insider (the exact adversary the threat model addresses) could race a symlink swap into the flagged path in the window between the pipeline classifying it and the enforcer acting, redirecting the read onto an attacker-chosen file — and D49/D57's atomic/idempotent correctness said nothing about WHICH file. Fix: `internal/enforcers/safeio` — `ReadRegularNoFollow` opens `O_RDONLY|O_NOFOLLOW` and requires `fstat` to show a REGULAR file (rejecting symlink/dir/fifo/device), reading via the fd; `RefuseNonRegular` (portable lstat) rejects a non-regular target before acting. encryptlocal reads through `ReadRegularNoFollow`; quarantine refuses a non-regular source outright. A swapped symlink is REFUSED — a loud, auditable enforcement failure (D14), never a silent redirect. Cross-platform: `//go:build unix` uses `syscall.O_NOFOLLOW`, a `!unix` fallback lstat-rejects a symlink (best-effort; the endpoint is Linux). Proven: a target swapped for a symlink to a secret is refused and the secret is neither read nor acted on. HONEST RESIDUAL: `O_NOFOLLOW` closes the FINAL-component swap; a PARENT-directory-component swap needs `openat2 RESOLVE_NO_SYMLINKS` (Linux 5.6+) or dir-fd-relative ops, and the strongest fix carries an fd from classification through enforcement (a pipeline change) — both documented follow-ups. Respects D49 (contain after detection), D16. Guards mutation-tested. | firm |
| **D66** | **The signed-telemetry sequence is now PERSISTED, so an agent restart is forward-monotonic instead of manufacturing false replay-rejections.** `SignedPublisher` held `seq` in memory with no persistence, so every restart (crash, redeploy, the `systemctl restart` D45 prescribes for upgrades) reset it to 0 — and `VerifySigned` rejects `seq <= last_sequence` as `ErrReplay`, so the next several legitimate messages after a restart were indistinguishable-from-tampering rejections until the counter organically climbed back. Routine restarts silently dropped telemetry and looked like an attack. Fix: a file-backed RESERVATION seq store persists a high-water mark in blocks (reserve N ahead, ~one atomic temp+rename write per N publishes, the D46 signer-state discipline); on startup `NewSignedPublisherWithSeq` LOADS the last reserved high-water and resumes there, never reusing a sequence. Resuming AHEAD is safe by construction: `VerifySigned` treats `seq > last+1` as a GAP (accepted + counted, D50), only `seq <= last` as a replay — so a persisted monotonic counter is always forward. A corrupt seq file fails LOUDLY (refuse to start) rather than silently resetting. Wired into the fleet agent via `OPENSHIELD_SEQ_FILE`; the in-memory constructor stays for tests/short-lived callers. Proven: sequences are monotonic across a restart (no reuse), and an integration test shows a restarted agent's telemetry is ACCEPTED, not rejected as a replay — the mutation that resets to 0 reproduces the original bug and the test catches it. HONEST TRADE: a crash loses up to N-1 reserved-but-unused sequences → a bounded GAP (the control plane records it — a crash IS a small suppression), the price of crash-safety without a per-message fsync. Separately corrected the honesty overclaim: `internal/transport/nats` documented itself as "over NATS JetStream" but uses only core NATS (at-most-once); the doc now says so and names the offline queue (D40) as the durability path — wired in #4b. Guards mutation-tested. | firm |
| **D67** | **The durable offline queue (D40) is now WIRED into the signed telemetry path and the fleet agent — realizing the offline-capable principle (D1) that was, until now, only unit-tested with zero production callers.** The queue was built and tested but the fleet agent published directly and discarded errors, so a laptop that woke with the control plane unreachable lost every signed message it produced (core NATS is fire-and-forget, D66) — the exact silent loss the whole system exists to prevent (D31). Fix: `SignedPublisher` gains an optional `Spool` (satisfied by `*queue.Queue`); `storeOrSend` publishes directly only when connected AND the spool is empty, otherwise durably ENQUEUES — preserving FIFO (a new message goes BEHIND anything queued, so a recovered connection does not let it race ahead); `Flush` drains in order, stopping at the first failure and keeping the tail. The SIGNED envelope bytes are stored and re-sent VERBATIM — the sequence and signature are baked in, so a late message verifies exactly as a live one (a gap at worst, D50, made safe by the persisted monotonic sequence, D66). The fleet agent opens a bounded queue at `OPENSHIELD_QUEUE_DIR`, attaches it, and Flushes each tick; an overflow eviction fires a HIGH-SEVERITY log — the honest guarantee is "no SILENT loss", not "no loss" (D31, bounded spool drops OLDEST loudly). Store-or-forward is behind injectable send/connected seams, so the logic is unit-tested deterministically without a live broker. Proven: telemetry produced during an outage is spooled (none lost) and delivered in order byte-for-byte on recovery; a connected publisher still queues behind a backlog; overflow is loud. Respects D1, D31, D50, D66. Guards mutation-tested. | firm |
| **D68** | **The endpoint engine — holder of the ledger signer key (D46) and OPA (D48) — is now installed and ISOLATED under its own dedicated user + systemd unit, closing the D45/D48 packaging gap.** `install.sh` built `openshield-engine` but installed no unit or user for it (and the anchor units D64 were uninstalled too), so a deployer ran the engine under whatever account was convenient — plausibly the very user being MONITORED, putting the signer key (0600) and OPA under no isolation, the erosion-to-the-monitored-user D45 warns about (D48 shipped after D45 and never closed the loop). Fix: `deploy/systemd/openshield-engine.service` runs under a dedicated non-login `openshield-engine` user (NOT root, NOT the monitored/other accounts), unprivileged (empty `CapabilityBoundingSet`, `NoNewPrivileges`, `ProtectSystem=strict`, `PrivateTmp`, `ProtectHome`) — notify-mode fanotify needs no capabilities (D52) — with a `StateDirectory=openshield-engine` (0700, owned by the engine user) holding the signer state: the key that signs the evidence is not under the account whose activity IS the evidence. The engine keeps network/DB access (pgx, OPA) — it parses no attacker bytes (the worker does, D29/D35), so that access is to Postgres, not attacker input. `install.sh` creates the engine user, installs the engine unit + the anchor service/timer, and enables the engine (the agent stays enabled-not-started). A build-time Go test parses the unit + install.sh and asserts the isolation (dedicated user, NoNewPrivileges, no CAP_SYS_ADMIN, a StateDirectory, engine+anchor units installed), so a regression fails the build. HONEST BOUND (D16): host root still defeats at-rest protection; this closes the WRONG-USER erosion, not a root compromise. Guards mutation-tested. | firm |
| **D69** | **The network data plane (N1) FITS the existing pipeline with only small, identifiable, additive core changes — the D26 fitness claim re-validated in code, the way peer-UEBA (D53) validated it.** Before building the inspection/enforcement gateway, the contract was settled: can Event/Decision/Enforcer REPRESENT a network flow and a network verdict without a rewrite? The additions are exactly three, all additive: (1) a `NetworkSubject` Event target variant (flow_id = an opaque handle into the gateway's live flow table = the enforce target; the 5-tuple; L7 metadata sni_host/method/path/direction) — METADATA ONLY, never the body (classified in the gateway process, D10/D29, as file content stays in the worker); added as a `oneof target` variant beside filesystem and usb, the SAME pattern USB used. (2) EventKind NETWORK_FLOW/HTTP_REQUEST. (3) `ACTION_REDIRECT` in the closed action set + one line in validate.go. DELIBERATE minimalism: block-vs-reset is an enforcement MODE (drop vs RST — how a flow enforcer denies), NOT a verdict, so it gets no action (keeps the vocabulary small, D14); the flow enforcer REUSES `core.TargetedEnforcer(ctx, dec, target)` with target = flow_id, so NO new enforcer interface. PROVEN in code: a network HTTP_REQUEST Event flows through the UNCHANGED `core.Dispatcher` with a network-classify stage + a policy reading host/path → emits REDIRECT, decided and audited via the existing OnOutcome sink, and a flow enforcer carries it out via target=flow_id — the Dispatcher, State, Stage, Registry, Enforcer interface, OnOutcome and ledger all untouched. Two existing completeness guards (`TestActionEnumIsClosed`, the policy action-mapping) fired and were updated deliberately — adding an action is never silent. NOT built (stated plainly): the proxy data plane — TLS interception, the flow table, the network classifier, the flow-enforcer implementation, the boundary-safe telemetry projection. This de-risks N1: the contract is proven data-plane-agnostic before the gateway is written. Guards mutation-tested. | firm |
| **D70** | **The network gateway WALKING SKELETON (N1.1) — the network analogue of the endpoint engine (D48/D62) — is built and proven end to end WITHOUT sockets, reusing the whole endpoint pipeline unchanged.** With the contract proven (D69), the first working proxy piece is the network pipeline ASSEMBLY: `internal/gateway.Gateway` takes a network `Request` (5-tuple, L7 metadata, and the plaintext `Body` the gateway holds), classifies the BODY in-process by REUSING `internal/classify` (the existing CPF/Luhn/etc detectors over the body bytes), projects the hits to a CONTENT-FREE `LocalClassification` (type+confidence+count, matched text NEVER attached — the exact projection the engine does from worker hits, D10/D29), builds a `NetworkSubject` Event (metadata only, one-way pseudonymised subject, D23), and runs it through the UNCHANGED `core.Dispatcher` (a per-request body-classify stage + the REAL OPA policy stage) with the EXISTING `core.NewAuditSink` for `OnOutcome` — the Decision lands in the forward-secure ledger. Observe-only by DEFAULT (D1): with no enforcer registered it decides and records and enforces nothing. `internal/enforcers/flow.Enforcer` implements the EXISTING `core.TargetedEnforcer` (a SECOND enforcement domain after files, interface unchanged), advertises BLOCK+REDIRECT, and resolves the `flow_id` target via a pluggable `FlowTable` seam (`Block`/`Redirect` by flow id — the socket-backed table is N1.2). `Gateway.enforce()` mirrors `engine.enforce`: `CanEnforce` gate, `EnforceTarget(ctx, dec, ev.GetNetwork().GetFlowId())` (flow_id is the network analogue of the file path), enforcement outcome audited, failure high-severity never silent (D14). PROVEN in code with no sockets and no Postgres (fake ledger, fake flow table): a request whose body carries a CPF → REAL policy → audited ALERT; BLOCK/REDIRECT route to the flow table by flow_id; observe-only default touches nothing. New code is `internal/gateway` + `internal/enforcers/flow` only — ZERO change to the dispatcher, State, Stage, Registry, the enforcer interface, OnOutcome, the ledger, or the proto (D69 fitness holds under load). NOT built (stated plainly): real sockets + a socket-backed `FlowTable` that drops/resets/redirects a live TCP flow (N1.2); TLS interception + the do-not-intercept list (N1.3). Guards mutation-tested (content-leak, wrong enforce target, action mis-routing). | firm |
| **D71** | **The gateway classifies attacker-controlled network bodies IN-PROCESS in the skeleton — a KNOWN gap that production MUST close by moving body classification to a sandboxed worker, not a safe assumption.** The endpoint deliberately parses untrusted file bytes in a seccomp-sandboxed, network-denied worker (D29/D35) because a parser bug in a network-capable process is RCE. The gateway IS network-capable (it is a proxy), so classifying untrusted bodies in the gateway process is the SAME danger by another door. D70's skeleton runs no live traffic and classifies in-process to keep the first increment small; before the gateway sees real traffic (N1.2), body classification MUST run in the sandboxed worker (reusing the D35 worker seam), so the process holding the network sockets never parses attacker bytes. Recorded loudly as a required hardening — the "mechanism proven in isolation, false of the assembled system" failure the audit round caught (D57-D61 pattern) demands this be a decision, not a silent gap. **CLOSED by D72.** | firm |
| **D72** | **Gateway body classification runs IN THE SANDBOXED WORKER, not in the gateway process — D71 closed before any live traffic.** The worker's `ClassifyRequest` gains a third `oneof subject` variant, `bytes content`, beside `path` and `file_handle`: a network-capable caller that ALREADY holds the body (the gateway read it off the socket to proxy it) hands the bytes to the worker, so the PARSER — the RCE surface, regex today and format parsers later — runs behind seccomp/no-network (D29/D35), not in the process holding the network sockets. This does NOT weaken the endpoint: the path-only invariant (the worker opens the file itself and the CAP_SYS_ADMIN agent never holds bytes) is the AGENT's discipline — its code sends path-only and `check-agent-deps` keeps parsers out of its binary — NOT the proto's; the new variant is exercised by the gateway, a different node that legitimately holds bytes. `worker.Handle` switches on the subject and classifies inline content through the SAME bounded `limitReader` (max_bytes ceiling → truncated flag) the file path uses — no second, weaker path; empty content is a clean no-hit result, no-subject is an error (D17). `internal/gateway` now depends on the classifier INTERFACE (the same one the engine holds; a `*privileged.Worker` satisfies both) and NO LONGER imports `internal/classify` — asserted by a `go list -deps` guard, the check-agent-deps discipline applied to the gateway. The body-classify stage sends `Content=body`, surfaces `resp.Error` as a stage failure (a worker error is not a clean result, D17), and builds the content-free `LocalClassification` from `resp.Hits` exactly as the engine does (matched text never crosses the IPC, D10/D29). `Worker.Classify` is mutex-serialized, so one shared worker is concurrency-safe now; a worker POOL for the concurrent listener is an N1.2b follow-up (noted, not hidden). PROVEN: a walking-skeleton test builds+starts the REAL sandboxed worker binary and a fake ledger (no Postgres), sends a CPF body as inline content, and gets an audited ALERT — the body parsed in the worker; a guard asserts the gateway sends inline Content (never a path); the deps guard asserts the gateway does not link the parser. Guards mutation-tested (Path-not-Content, worker-error-ignored, gateway-links-parser, content-ignored). NOT in scope (stated): the real HTTP proxy listener + socket-backed FlowTable (N1.2b); TLS interception (N1.3). | firm |
| **D73** | **The network gateway DATA PLANE (N1.2b) is a plain-HTTP forward-proxy connector that carries the verdict to the LIVE connection via a socket-backed flow table.** With the pipeline (D70) and worker-classified bodies (D72) in place, this is the connector — the network analogue of the fanotify connector (D62). `internal/gateway.Proxy` (an `http.Handler`) per request: mints a flow_id, reads the body BOUNDED (buffered — it both classifies and forwards; a body over the cap is refused 413, not truncated, since truncation would forward a corrupt request), registers the flow, runs `gateway.Process` (classify in the worker → decide → audit → route BLOCK/REDIRECT through the table), then applies the flow's DISPOSITION to the connection: allow → forward upstream via an `http.RoundTripper` and copy the response back; block → 403; redirect → 302 to a coaching URL. **The verdict reaches the connection as a disposition, NOT by the enforcer closing the socket** — `internal/gateway.Table` (the socket-backed `flow.FlowTable`) records allow|block|redirect and the OWNING handler applies it, because the handler owns the connection lifecycle and an enforcer closing the socket would race it; block-vs-reset (403 vs RST) is then a handler choice, the enforcement MODE D69 kept out of the verdict vocabulary. A verdict for an unregistered (not-live) flow is an error, surfaced not swallowed. **Observe-only by DEFAULT (D1):** the flow enforcer is registered only when `OPENSHIELD_ENFORCE` is set; off, a BLOCK decision leaves the disposition at allow and the flow is forwarded and merely audited — observe-first egress monitoring, a deployer opts INTO blocking. **Fail-OPEN on a pipeline error, loudly (D17/D18):** `Process` already audits the failure via the outcome sink, so the proxy forwards the flow with a high-severity log rather than turning a classifier failure into a denial of service on all egress (fail-closed is a noted config follow-up). `cmd/openshield-gateway` is a thin binary (validate config, `StartWorker`, open the Postgres ledger, `NewFromWorker` + Table + Proxy, `http.Server`) and, like the engine, spawns the worker rather than parsing — its dependency graph excludes `internal/classify`, guarded by the extended `go list -deps` test (D72). PROVEN on REAL sockets with no Postgres (fake ledger): an httptest upstream + the Proxy on `httptest.NewServer` + a real proxy client — a clean flow is forwarded (upstream hit, 200), a BLOCK flow gets 403 with the upstream NEVER hit, REDIRECT yields 302 to the coaching URL, observe-only forwards+audits a BLOCK decision, and a worker error fails open (forwarded, failure audited). Guards mutation-tested (block-forwards, fail-open→fail-closed, unregistered-verdict). NOT in scope (stated): TLS interception + do-not-intercept list (N1.3); the worker POOL (one mutex-serialized worker is correct — a throughput follow-up); active teardown of long-lived/streaming flows. | firm |
| **D74** | **The gateway proxy handles CONNECT with a BLIND TCP tunnel — HTTPS transits the proxy but is NOT inspected (the groundwork before TLS interception).** The D73 proxy handled only plain-HTTP absolute-URI requests; an HTTPS client first sends `CONNECT host:443`, so without this HTTPS did not work through the proxy at all. `Proxy.ServeHTTP` now dispatches CONNECT to `handleConnect`: dial the upstream (`net.DialTimeout`, 502 on failure), hijack the client connection (`http.Hijacker`), reply `200 Connection Established`, and relay bytes both directions with two `io.Copy` goroutines that close both ends when either finishes. The TLS session is end to end between the client and the origin, so the proxy relays CIPHERTEXT and classifies NOTHING — tunneled HTTPS bodies are a stated coverage gap, LOGGED per tunnel ("not inspected — TLS interception is N1.3b") so the gap is operationally visible, not silent (D16). Plain-HTTP handling (D73) is unchanged. PROVEN with REAL TLS: an httptest TLS upstream + the Proxy + a real proxy client trusting the upstream cert — an HTTPS POST succeeds end to end through the tunnel AND the ledger stays EMPTY (the tunneled body was not classified — the honest gap); a CONNECT to a dead upstream returns 502; plain-HTTP forwarding still works. Guard mutation-tested (removing the CONNECT dispatch breaks the tunnel and leaks a ledger entry). NOT in scope, and deliberately deferred with its reasoning recorded: TLS INTERCEPTION (terminate the inner TLS by presenting a leaf the client trusts, classify the inner HTTP request through the existing pipeline, re-forward over upstream TLS) + the do-not-intercept list = N1.3b, and it MUST use a SEPARATE interception CA — never the fleet mTLS CA (D60), because a MITM root can impersonate the whole internet to the fleet, a far larger blast radius than fleet identity needs; a metadata-only audit of tunneled flows is a noted visibility follow-up; the worker POOL is a throughput follow-up. | firm |
| **D75** | **TLS INTERCEPTION closes the HTTPS coverage gap: the gateway terminates the client TLS with a leaf minted on the fly (signed by a SEPARATE interception CA), classifies the inner HTTP request through the SAME pipeline as plain HTTP, and re-forwards over origin TLS — with a do-not-intercept list that tunnels pinned/sensitive hosts blind.** D74's tunnel made HTTPS transit the proxy uninspected; this inspects it, opt-in. Pieces: (1) `provision.InterceptionCA()` — a CA generator DELIBERATELY DISTINCT from the fleet mTLS CA (D60): an interception CA signs a trusted cert for ANY host, so it can impersonate the whole internet to every endpoint that trusts it — a far larger blast radius than fleet identity; fusing them would give the fleet CA that power. Its custody IS interception's security (D16), like the escrow/signer keys. (2) `internal/gateway.CertMinter` — a `tls.Config.GetCertificate` callback minting (and caching) an Ed25519 leaf per host (SNI, or the CONNECT host when the client sends no SNI — an IP literal — with an IP SAN in that case); empty host rejected. (3) A do-not-intercept matcher (exact host + domain suffix): cert-pinned apps BREAK under MITM and banking/health egress is sensitive, so exclusion is a correctness AND privacy control, the safe default; a match tunnels blind (D74) even with interception on, and no CA configured tunnels everything. (4) `Proxy.handleConnect` intercepts a non-excluded host when a minter is configured, else tunnels; `intercept()` hijacks, terminates the client TLS (`NextProtos:["http/1.1"]`), and serves the decrypted conn with `http.Serve` over a one-shot listener whose served conn closes the listener on close (so Serve returns only when the connection is done, never mid-request), reconstructing `https://<host><uri>` and running the SAME `serve()` as plain HTTP. (5) `ServeHTTP`'s body refactored into `serve(w, r, targetURL, rt)` shared by both paths, so classify-in-worker (D72) → decide → audit → disposition (forward/block/redirect) and fail-open (D17/D18) are IDENTICAL for intercepted HTTPS and plain HTTP — one code path, one set of guards. (6) `cmd/openshield-gateway` enables interception only when a CA is configured (`OPENSHIELD_INTERCEPT_CA_CERT/KEY`), with the list from `OPENSHIELD_NO_INTERCEPT`; observe-only + tunnel stay the defaults, and enabling logs a loud warning. PROVEN with REAL TLS end to end (in-test interception CA trusted by the client, in-test TLS origin trusted by the gateway's origin transport): an intercepted CPF body is CLASSIFIED (a ledger entry — D74 gap CLOSED) and forwarded to the origin with the minted leaf trusted by the client; a BLOCK inner request → 403 with the origin NEVER hit; a do-not-intercept host is tunneled (no ledger entry). Guards mutation-tested (ignore-the-exclusion-list, intercept-skips-the-pipeline, no-IP-SAN, empty-SNI). Interception reads employees' HTTPS — it is opt-in, scoped by the do-not-intercept list, and subject to the notice/DPIA posture the project documents; not hidden. NOT in scope (stated): revocation/rotation for the interception CA; HTTP/2 and QUIC interception; a metadata-only audit of tunneled flows; the worker pool. | firm |
| **D76** | **The gateway classifies concurrent flows across a WORKER POOL — parallel parsing without changing the worker or the classifier interface.** `privileged.Worker.Classify` holds a mutex (one framed request in flight), correct for the endpoint engine (one event at a time) but a bottleneck for the concurrent HTTP proxy (D73), which serialized every body through one worker. `privileged.Pool` runs N worker processes behind the SAME `Classify(ctx, *ClassifyRequest)` method, so it is a drop-in for the `classifier` interface (the gateway's cmd swaps `StartWorker`→`StartPool` in one line; the engine keeps one worker). A BUFFERED CHANNEL of idle workers IS the semaphore: `Classify` receives one (blocking with backpressure when all N are busy, selecting on `ctx.Done()` so a cancelled request doesn't wait forever), classifies, releases it — in-flight parses bounded by the pool size, each still in its own seccomp/no-network sandbox (D29/D35, D72). On ANY `Classify` error the worker's framed-IPC state is unknown (a timeout desyncs the protocol, a crash kills it), so the pool DISCARDS and REPLACES it — a poisoned worker never serves a later request — while still returning the error to the caller (D17: surfaced, not a clean result); if the replacement spawn fails the old handle is returned so the slot isn't lost (next use retries) rather than shrinking toward deadlock. PROVEN with the REAL worker binary under `-race`: 40 concurrent inline-Content classifications on a size-4 pool all return the CPF hit (requires release to work, else deadlock — bounded in-test), a size-1 pool works, and a cancelled-context acquire returns promptly. Guards mutation-tested (no-release → deadlock caught; ignore-ctx → cancelled-acquire hang caught). `OPENSHIELD_WORKER_POOL` (default 4). NOT in scope (stated): dynamic resizing; health checks beyond replace-on-error; a shared cross-process pool. | firm |
| **D77** | **The gateway projects a BOUNDARY-SAFE network telemetry record to the control plane — redacted destination+verdict, never the user IP or the URL path — so the fleet view sees network decisions.** The gateway records every Decision to its local forward-secure ledger (the system of record, D30), but the control plane had no visibility into network verdicts. After a Decision is produced and recorded, if a telemetry projector is configured the gateway publishes a REDACTED network Event + the Decision through the existing signed transport (`natsx.SignedPublisher`, signed with an enrolled identity, verified and persisted by the control plane, D41/D44; offline-queued, D67). The REDACTION is the crux (D10/D29 + the D69/D73 DPIA concern): `redactForTelemetry` CLONES the Event (never mutating the local record) and, on the NetworkSubject, CLEARS `src_ip`/`src_port` (user-identifying — the Event already carries a pseudonymous Subject, D23) and `http_path` (path+query carries tokens/emails/search terms — content-like), while KEEPING the destination (`dst_ip`/`port`, `sni_host`), `http_method`, `protocol`, `direction`, `flow_id` — so the server learns "a flow to upload.evil.com was BLOCKED" without the user's IP or the sensitive URL. The Decision is projected as-is (schema-guarded to carry no content/URL/detector internals, D14). Projection is OPT-IN (nil projector = off, the default, via a narrow `Projector` interface — the gateway does not own the transport's lifecycle), ADDITIVE (never the system of record), and BEST-EFFORT (a publish error is logged, never fails the request — the decision is already recorded locally and the publisher offline-queues). `cmd/openshield-gateway` wires it when NATS + an enrollment endpoint are configured (the shared `enroll` helper, extracted from the fleet agent so both binaries reuse it). PROVEN with a fake projector (no NATS): a network Process projects one redacted Event (empty src_ip + http_path, non-empty sni_host + dst) + one Decision; nil projector projects nothing; a publish error does not fail Process and the local ledger still recorded. Guards mutation-tested (no-redaction leak; project-when-nil). NOT in scope (stated): projecting the ClassificationSummary (needs the classification plumbed out of Process); full-URL projection under a policy opt-in; async projection. | firm |
| **D78** | **Tunneled (uninspected) flows are recorded as a METADATA-ONLY ledger entry, so uninspected egress is VISIBLE in the audit trail rather than silent.** The gateway tunnels HTTPS blind (D74, no interception CA) and tunnels do-not-intercept hosts (D75) — previously recording nothing, so an investigator could not distinguish "a flow to evil.com transited uninspected" from "no flow happened" (the silence the audit trail exists to prevent, D1/D17). `Gateway.RecordTunnel(host, reason)` appends a `core.Entry` with `OutcomeKind="tunneled"` and `OutcomeStage="<host> (<reason>)"`, a NIL Decision — a tunnel is an OUTCOME, not a verdict; representing it as an ALLOW Decision would falsely claim it was inspected. It carries the destination host and the reason it was not inspected ("interception-disabled" or "do-not-intercept") and NOTHING else — no body, no URL path (a CONNECT has neither), so it is inherently boundary-safe (D10/D29). `Proxy.handleConnect` records one entry per tunneled CONNECT (per TLS connection, not per request) before relaying. Best-effort: an append failure is logged and the tunnel still proceeds — the flow is happening regardless, and a recording failure must not sever connectivity. This UPDATED the D74/D75 tests that asserted zero ledger entries for tunneled flows: they now assert exactly one metadata-only "tunneled" entry naming the host. PROVEN: a blind tunnel records one "tunneled" entry (reason interception-disabled, host named, no Decision); a do-not-intercept host records one (reason do-not-intercept); an INTERCEPTED flow records its Decision and NO tunnel entry (the paths are distinct); the tunnel still works when the append fails. Guards mutation-tested (no-record, wrong-reason, tunnel-entry-carries-a-decision). NOT in scope (stated): projecting tunnel records to the control plane (a decision-less telemetry path — a follow-up); per-request records; inner-ClientHello SNI capture. | firm |
| **D79** | **The interception CA is HOT-ROTATABLE, and leaf/CA revocation in the minimal PKI is stated honestly.** The interception CA (D75) is a skeleton key that can impersonate any site, and the minimal PKI (D60) has no CRL/OCSP/rotation — so "we can never replace it without a restart" was a sharp gap. `CertMinter.Rotate(caCertPEM, caKeyPEM)` VALIDATES the new CA first, then under the lock atomically swaps the CA AND FLUSHES the leaf cache (every cached leaf chains to the OLD CA, so a stale leaf signed by a rotated-away — possibly compromised — CA is never served after rotation). A bad rotation (invalid PEM, a half-written reload) returns an error and keeps the WORKING CA — fail-safe, because a minter that could no longer mint would fail every intercepted handshake; interception is never silently disabled. `cmd/openshield-gateway` hot-rotates on SIGHUP (reload the CA files → Rotate) without dropping connections; a reload error is logged and the old CA keeps serving. **Revocation posture (minimal PKI):** the SHORT leaf TTL (`leafTTL`, ~24h, now a named const with the reasoning next to it) IS the leaf-revocation mechanism — ephemeral per-host leaves, minted fresh, cached only in memory, self-limit by expiry, and a rotation flushes them at once, so no CRL/OCSP is needed for leaves; CA-level revocation is rotate-away (`Rotate` to a fresh CA, discarding the old key) or remove-to-tunnel (drop the CA config → interception off, D74); and removing a compromised CA from ENDPOINT trust stores is the endpoint's job, out of the gateway's scope (D16 — the gateway change bounds the gateway's behaviour, not the fleet's trust). PROVEN with real certs: after `Rotate` to CA2 a re-minted leaf chains to CA2 and NO LONGER verifies against CA1 (cache flushed + CA swapped); an invalid `Rotate` errors and the minter still mints under CA1; `Rotate` is race-safe with concurrent `For`. Guards mutation-tested (no-cache-flush, swap-before-validate). NOT in scope (stated): CRL/OCSP for leaves; automated rotation scheduling; endpoint trust-store distribution/removal; gateway-side multi-CA overlap trust. | firm |
| **D80** | **The endpoint engine now PROJECTS real detections to the control plane — closing the engine-to-fleet disconnect an adversarial audit surfaced.** The audit's standout finding: `openshield-engine`, the only component producing REAL endpoint detections, wrote to its LOCAL ledger and never published telemetry, so the control plane's verified stream had only ever carried the fleet-agent SIMULATOR's synthetic events + gateway projections (D77) — fleet visibility, peer-UEBA (D53/D54) and the dead-man's-switch (D50/D51) had NEVER operated over a single real endpoint detection (the recurring "verifies against its own assumptions" pattern, D62, still live at the fleet boundary). Fix, mirroring the gateway (D77): `engine.Engine` gains an optional `Projector` (PublishEvent + PublishDecision — narrow, engine-local for clean endpoint/network layering; a `SignedPublisher` satisfies it) + `SetTelemetry`; `Process` projects the Event + Decision after the local record, best-effort (a publish error is logged, never fails Process — the local forward-secure ledger is the system of record, D30, and the publisher offline-queues, D67). The Event is projected AS-IS: its filesystem path is the FILE's IDENTITY (the fleet investigation needs "which file on which endpoint") and the Subject is already pseudonymous (D23) — distinct from the gateway, which redacts the URL path because a URL path is content-like (D77). The Decision carries no content (D14). `cmd/openshield-engine` wires a `SignedPublisher` (enrolled identity + NATS, optional seq file) when `OPENSHIELD_NATS_URL` + `OPENSHIELD_ENROLL_URL` are set, using the shared `enroll` helper — exactly as the gateway does; off by default, so the single-host observe path is unchanged. PROVEN with a fake Projector (no NATS): a file event Process projects one Event (path retained) + one Decision; nil projects nothing; a publish error does not fail Process and the local ledger still recorded. Guards mutation-tested (project-when-nil, no-event-projection). NOT in scope (stated): retiring the fleet-agent simulator (still useful for control-path e2e); path redaction (a configurable follow-up); ClassificationSummary projection. | firm |
| **D81** | **Retention purge is now SCHEDULED and running — closing a firm-decision contradiction (D20) the audit surfaced: retention was built but never called.** `Ledger.Purge` (the forward-secure ledger's retention tombstoning, T-013) had ZERO production callers, and the fleet aggregate (`fleet_telemetry`, `peer_alerts`) had no retention at all, so as deployed OpenShield retained personal-adjacent telemetry INDEFINITELY — the exact posture D20's GDPR reasoning forbids. Fix: (1) `controlplane.Server.PurgeOlderThan(ctx, cutoff)` hard-DELETEs the fleet aggregate (received telemetry by `received_at`, derived peer alerts by `detected_at`) past the cutoff. A hard delete is correct HERE because the fleet aggregate is a DERIVED, re-derivable detection view, NOT the evidentiary ledger (D38); the forward-secure ledger instead TOMBSTONES (content erased, chain skeleton kept verifiable, D36) — using the wrong mechanism on either would be a bug (deleting from the ledger destroys verifiability). (2) `internal/retain.Loop(ctx, interval, fn)` — one cancellation-aware ticker so the three binaries don't duplicate the loop. (3) `cmd/openshield-server` schedules `PurgeOlderThan(now - OPENSHIELD_FLEET_RETENTION [90d])` every `OPENSHIELD_RETENTION_INTERVAL [24h]`, logging rows removed; `cmd/openshield-engine` and `cmd/openshield-gateway` schedule `ledger.Purge(now)` on the same timer — finally running the tombstoning D20 promised. PROVEN against real Postgres: fleet_telemetry + peer_alerts rows older than a cutoff are deleted and newer rows remain; `retain.Loop` ticks then stops on cancellation and is disabled on a non-positive interval; the ledger tombstoning is already covered by the existing postgres_test. Guards mutation-tested (inverted cutoff; skip-a-table). NOT in scope (stated): per-subject/per-purpose retention (one fleet window); a legal-hold exemption surface (the unbounded investigation class is the coarse hold); changing the ledger's class-based tombstoning. | firm |
| **D82** | **The operator now has a READ API over the fleet — closing the "zero readers" gap the audit found.** The two things a security team most needs to see — peer-UEBA detections (`peer_alerts`) and silent/overdue agents (`Server.Overdue`) — had no read API: `peer_alerts` was read only by `psql` in tests and `Server.Overdue` had zero production callers, so the dead-man's-switch (D50/D51) and peer-UEBA (D54) fired into a void. `Server.RecentPeerAlerts(ctx, limit)` reads recent alerts (pseudonymous subject, D23; no content) and `Server.OperatorReadHandler()` serves GET `/alerts` (JSON, `?limit=` default 100) and GET `/overdue` (`Overdue(threshold, now)` JSON, `?threshold=` default 15m). Both mount in `ServeHTTPTLS` behind `requireRole(RoleOperator)` — the SAME mutual-TLS cert-role gate `/view` uses (D56/D58): an operator cert gets 200, an agent cert 403, and (with the broker requiring client certs) an unauthenticated peer never completes the handshake. The endpoints are READ-ONLY and hold no signer — they can forge nothing (the D30 read/write asymmetry that lets a reader run without threatening the evidence). PROVEN with real Postgres + the operator-cert harness: a seeded peer alert and a stale agent are returned as JSON to an operator, and an agent-role cert is refused 403 on both. Guards mutation-tested (no-role-gate → agent not refused; limit-0 → empty). Still no UI — but this JSON API is the substrate a UI would sit on. NOT in scope (stated): a UI; an operator-read audit row for aggregate reads (distinct from per-subject /view investigation audit, D47 — a noted follow-up); pagination/filtering beyond limit+threshold; a telemetry search API. | firm |
| **D83** | **Alerts are DELIVERED to a human, not only logged — closing the "no alert delivery" gap the audit found.** No webhook/SMTP/pager code existed anywhere; a high-severity alert was a slog line or a DB row, so a security team was never TOLD anything — it had to poll with psql. `internal/notify` provides a `Notification` (Kind peer-alert | agent-overdue; pseudonymous Subject/AgentID per D23; risk score; time), a `Notifier` interface, a `Webhook` (POST JSON to a URL, short timeout — a deployer bridges it to Slack/PagerDuty/email with an off-the-shelf receiver, no vendor coupling), and a `Nop` default (delivery opt-in). `controlplane.Server` gains a notifier + `SetNotifier` and a best-effort `emit` helper: a delivery error is logged and COUNTED (`NotifyFailures`), NEVER propagated — a down webhook must not break telemetry ingest or the detection, because the alert is already recorded and delivery is additive (D30). `observePeer` notifies on a recorded peer alert (riding its existing per-subject cooldown). `Server.NotifyOverdue(threshold)` notifies on agents silent past the threshold (the dead-man's-switch, D50/D51), DEDUPLICATED against an in-memory set so a long-silent agent pages ONCE, not every interval, and can alert again after it recovers — the pure `newlyOverdue(prev, current)` computes the delta. `cmd/openshield-server` wires a `Webhook` from `OPENSHIELD_ALERT_WEBHOOK` and schedules `NotifyOverdue` via `retain.Loop`. PROVEN: the Webhook POSTs the Notification JSON to an httptest server which receives the right kind+fields (and a non-2xx is an error); `newlyOverdue` dedups across rounds and re-arms a recovered agent; `NotifyOverdue` against real Postgres + a fake notifier notifies a stale agent once, dedups the second check, and re-alerts after recovery+silence. Guards mutation-tested (no-dedup; next-never-tracks-current). NOT in scope (stated): SMTP/Slack/PagerDuty adapters (bridge the webhook); routing/severity/escalation; retry/queue (best-effort — the alert is still recorded, readable via D82); per-decision notifications (too high-volume). | firm |
| **D84** | **Deploy honesty + gateway hardening — the audit's remaining ship-blockers.** Three fixes. (1) `install.sh` ENABLED `openshield-agent.service`, but the agent is an honest STUB that exits non-zero (the deferred inline-blocking component, D49/D62), so systemd looped it as a failed unit; it is now installed-but-NOT-enabled, with the operator note stating plainly it is a deferred stub — a `systemctl enable` no longer runs a guaranteed-failing service (guarded by a packaging test so the stub is never silently re-enabled). (2) The gateway holds the ledger signer (D46) AND — when interception is on — the interception-CA private key, a SKELETON KEY that can impersonate any site (D75), yet unlike the engine (D68) it had no unit and no isolation. It now runs under `deploy/systemd/openshield-gateway.service`: a dedicated non-login `openshield-gateway` user (never root, never the monitored account, D45), empty CapabilityBoundingSet, NoNewPrivileges, ProtectSystem=strict, ProtectHome, PrivateTmp, and a 0700 StateDirectory holding the signer state + CA files; `install.sh` creates the user and installs + enables it. It keeps network access (it is a proxy — network denial is the worker's role, D35). Host root still defeats at-rest (D16); this closes the wrong-USER erosion, matching the engine. (3) The brief's "Zero Trust oriented" is REFRAMED (see Superseded): the gateway authenticates no subject — the network subject is `sha256(src-IP)`, not an identity — so it is NOT a ZT enforcement point, and it inspects only proxied HTTP(S), so it is NOT a NIPS; a negated README note states this (doccheck-safe, D37). PROVEN by packaging tests: the gateway unit isolates its secret-holder (dedicated user, NoNewPrivileges, empty caps, ProtectSystem=strict, StateDirectory); the installer installs+enables the gateway + creates its user and does NOT enable the stub agent. Guards mutation-tested (re-enable-stub-agent; gateway-as-root). NOT in scope (stated): building the real inline-blocking agent (D49); ZT identity at the proxy (the architecture roadmap); packaged .deb/.rpm (still source-build). | firm |
| **D85** | **The Zero-Trust identity CONTEXT contract — core.Context carries typed identity, role, and device posture, and policy can decide identity-aware authorization through the UNCHANGED dispatcher (Phase-A foundation, contract-first D69-style).** Before building the identity PRODUCER (proposal §5.2), the contract is settled and PROVEN in code — exactly as D69 proved the network Event/verdict contract before the proxy. `core.Context` (D28) gains three TYPED fields (never a map — a compromised control plane cannot invent keys, D14's threat by another door): `Identity` (verified pseudonymous subject, D23), `Role` (authorization group), and `DevicePosture` (a closed struct: `HasPosture`, `Compliant`, `DiskEncrypted`, `AgentPresent`, `OSPatchTier PatchTier`). CRITICAL D28 discipline: `HasPosture` distinguishes "not computed" from "computed and compliant" — like `HasRiskScore` — because a defaulted "compliant" when posture is ABSENT is a silent fail-open. For a ZT ACCESS decision the safe direction on absence is CLOSED (deny): a killed/tampered endpoint reports no posture, `has_posture` is false, and the policy denies at the gateway — the endpoint the user controls cannot grant itself access by disabling its own agent (the tamper-lockout discussed as the answer to "can't the user bypass the host rule?"). `policy.buildInput` exposes `input.context.{identity, role, device_posture:{has_posture, compliant, disk_encrypted, agent_present, os_patch_tier}}` — a boundary-safe closed projection extending the existing risk block (D53), never the whole Context. PROVEN (fitness test, peer_test.go's D26 shape): an identity-aware rego over the UNCHANGED `core.Dispatcher` with a `ResolveContext` hook decides a compliant finance device → ALLOW, a NO-posture device → BLOCK via the absent-posture rule (tamper-lockout), and a present-but-non-compliant device → BLOCK — with ZERO change to the dispatcher, State, Stage, or Enforcer interfaces. The reason string discriminates absent-posture from non-compliant so masking absence is caught, not just the action. Guards mutation-tested (device_posture not exposed; has_posture masked present). NOT in scope (stated): the identity producer / OIDC / client-cert auth layer (§5.2); the reverse/access-proxy mode (§5.1); the internal-service catalog + microsegmentation (§5.4); the risk-publish continuous-verification channel (§5.5) — later Phase-A increments building on this contract. | firm |
| **D86** | **The client-certificate identity PRODUCER — a verified, pseudonymous subject + role fills the D85 context, replacing the sha256(src-IP) non-identity (Phase A.2).** D85 settled the identity CONTRACT; this produces a real verified subject to put in it. `provision.RoleClient` + `IssueClientCert(ca, identity, group)` is a DISTINCT issuance path beside `IssueCert` (unchanged, agent/operator only): an Ed25519 leaf with CN = the identity, OU = [RoleClient] (the marker — a client cert can never be mistaken for an agent/operator cert at the D58 role gate), O = [group] (the authorization class), clientAuth only, signed by the provisioning CA (D60). `internal/gateway/identity.FromClientCert(leaf)` verifies the leaf carries the RoleClient OU (rejecting an agent/operator/unknown cert — a client cert is a DISTINCT role, D58, verified never inferred), then resolves `Identity{Subject, Role}`: the CN is the raw user identity, so it is hashed ONE-WAY into `Subject = "sub_"+hash(CN)` (D23 — the raw identity never enters the pipeline, Context, or telemetry; the reverse mapping is a deployer concern behind an audited lookup, D23/D47), while the group is carried in the clear as `Role` (an authorization class, not personally identifying — a policy needs it). `Identity.Context()` builds a `core.Context{Identity, Role}` with `DevicePosture.HasPosture=false` — posture is a SEPARATE producer, so a cert-authenticated but unattested device still FAILS CLOSED under the D85 access policy (authentication is not device trust). PROVEN with real certs: a client cert for "alice@corp"/"finance" resolves to a pseudonymous subject (the raw "alice"/"corp" does NOT appear) + role "finance"; an agent and an operator cert are each rejected specifically by the client-role check; the produced Context has identity/role set and posture absent. Guards mutation-tested (no-role-check → non-client accepted; no-pseudonym → identity leak; posture-forced-present → fail-closed broken). This closes the "Zero Trust authenticates no subject" overclaim (D84) at the producer level. NOT in scope (stated): OIDC/bearer auth (A.2b, the lower-friction path for humans; client certs suit workloads); the reverse/access-proxy mode that calls this per connection (§5.1/A.3); the device-posture producer (a separate resolver feeding DevicePosture); the internal-service catalog (§5.4); the risk-publish loop (§5.5). | firm |
| **D87** | **The identity-aware ACCESS-PROXY mode — a BeyondCorp-style access broker that authenticates a client cert, authorizes per-request on the verified identity through the unchanged pipeline, and FAILS CLOSED (Phase A.3).** D85 gave the identity context, D86 the client-cert producer; this wires them into a live reverse/access proxy. `gateway.Request` gains an optional `Identity *core.Context` (nil for egress, unchanged); `Gateway.Process` resolves it via the dispatcher's `ResolveContext` hook (the SAME D53 seam peer-UEBA uses) so the policy authorizes on `input.context.{identity, role, device_posture}` (D85), and `toEvent` stamps `req.Identity.Identity` (the verified pseudonym, D86) as the Event Subject — finally replacing the `sha256(src-IP)` non-identity (D84) with a real verified subject in the pipeline and telemetry. `gateway.AccessProxy` (http.Handler): require a verified client cert (401 if absent), resolve via `identity.FromClientCert` (403 if not a client identity), run `Process` on the identity context (authorization AND request-body DLP), and ALLOW → reverse-proxy to the internal service (`httputil.ReverseProxy`, buffered body reset), else → 403. **ACCESS FAILS CLOSED** — a pipeline error DENIES (403), the deliberate OPPOSITE of the egress forward proxy which fails OPEN (D73/D17): egress fail-open preserves availability of monitored traffic, but access fail-open would grant entry on an error, the one thing a Zero-Trust gate must never do; the two directions protect opposite things and fail in opposite directions. NOTE: access policies are default-DENY (a ZT principle — an unmatched request denies), the opposite of the observe-first egress default-ALLOW (D1), so an access policy must explicitly ALLOW authorized roles. PROVEN with REAL TLS + real client certs (RequireAndVerifyClientCert): a finance client cert under a finance-authorizing policy reaches the httptest internal service and returns; a sales cert → 403 with the service NEVER hit; a pipeline error → 403 (fail closed, service never hit); and a capturing-policy unit test confirms the Event subject is the verified pseudonym (not the src IP). Guards mutation-tested (fail-open-on-error; ignore-the-verdict; ignore-the-identity-subject). NOT in scope (stated): the internal-service CATALOG + per-service microsegmentation policy + binary access-mode config (A.4 / §5.4, single fixed upstream here); OIDC/bearer auth (A.2b); the device-posture producer (posture stays absent → a posture-requiring policy still denies, D85); the risk-publish continuous-verification loop (§5.5 / A.5). | firm |
| **D88** | **The internal-service CATALOG + per-service MICROSEGMENTATION — the access proxy fronts many internal services and authorizes per service on the verified identity (Phase A.4).** D87's access broker fronted a single upstream; this makes it a real segmented access gateway. `gateway.Catalog` is an explicit ALLOW-LIST of internal services (`Add(name, upstream)` stores a per-service `httputil.ReverseProxy`; `Resolve(host)` routes by exact host). `AccessProxy` takes the catalog: after client-cert auth (D87), it resolves the requested service from the Host — an UNKNOWN service is refused 404 and NEVER forwarded (the gateway is an allow-list, not an open relay to arbitrary internal hosts, which would be an SSRF/pivot surface) — sets `Request.Host` to the resolved service so the pipeline Event carries WHICH service is targeted, runs `Process` (authorize per-service on the identity context), and ALLOW → reverse-proxy to THAT service, else → 403; the D87 fail-CLOSED-on-error and client-cert auth are unchanged. `policy.buildInput` now exposes `input.event.{host, method, path}` for a network Event so the policy can microsegment (`allow if role=="finance" and event.host=="payroll"`) — identity-based, replacing IP-based ACLs (D84). This reaches only the LOCAL in-process policy: telemetry still REDACTS the URL path (D77) and the Decision carries no content (D14), so local exposure is not a boundary crossing. PROVEN with real TLS + real client certs: a catalog of {payroll, wiki} and a policy allowing finance→payroll only — the SAME finance identity REACHES payroll (200, payroll upstream hit) but is DENIED wiki (403, wiki upstream NEVER hit), the core microsegmentation property (same identity, different service, different verdict); an unknown service host → 404, no upstream reached. Guards mutation-tested (no-unknown-service-refusal → open relay; event.host-not-exposed → microsegmentation fails). NOT in scope (stated): the binary access-mode wiring + file-loaded access policy + env catalog config (A.4b); OIDC (A.2b); the device-posture producer; the risk-publish loop (§5.5 / A.5); path-prefix or wildcard routing (exact host match here). | firm |
| **D89** | **Risk-driven CONTINUOUS VERIFICATION — the gateway reads published risk and the LOCAL policy cuts access on it, closing the D54 dead-end (Phase A.5, the T2 model the owner confirmed).** Peer-UEBA computes a per-subject risk score (D53/D54) that reached NO enforcement point — D54 said plainly "no risk is fed back". This wires it to the access gateway. `gateway.RiskStore` holds the latest PUBLISHED per-subject risk (`Set`/`Get`; the server->gateway signed publish channel is A.5b); `AccessProxy.SetRiskStore` enriches each request's identity Context with the connecting subject's risk before `Process`, so the policy sees `input.context.risk_score` (D85) and can step-up (REDIRECT) or deny (BLOCK) a subject whose risk crossed a threshold MID-SESSION — decided by the LOCAL policy reading risk as DATA, the server never commanding (T2/D14: a compromised server can publish a WRONG score, a lie the bounded local policy still evaluates, but it cannot COMMAND the gate — the property that lets continuous verification exist without reopening the control-plane-actuates threat). ABSENT risk is NOT high — the OPPOSITE fail-direction from posture (D85): absent posture fails CLOSED (an unattested device is untrusted), but absent risk means "analytics is quiet" not "danger", so a missing score does not deny (denying the fleet on an analytics hiccup would be the D28 fail-open-with-no-signal trap in reverse); `HasRiskScore` distinguishes the two, and posture/risk fail in different directions because they mean different things ("is this device trusted" vs "is this subject anomalous"). High risk reuses BLOCK/REDIRECT — no new action (a dedicated STEP_UP is a noted T1 option). PROVEN with real TLS + real client certs: a policy authorizing role finance but blocking risk >= 0.8 — with NO risk published the finance client REACHES the service; after `RiskStore.Set(subject, 0.95)` the SAME client is DENIED 403 with the service NEVER reached (access cut mid-session by the local policy reading published risk). Guards mutation-tested (no-risk-enrichment; Get-always-absent). NOT in scope (stated): the server->gateway signed risk-publish channel (A.5b, reuses D50 transport in reverse); a dedicated STEP_UP action (T1); the device-posture producer; OIDC (A.2b); egress-path risk gating. | firm |
| **D90** | **The Zero-Trust access gateway is RUNNABLE — cmd/openshield-gateway ACCESS MODE serves the proven AccessProxy (D87/D88/D89) as a binary (Phase A.4b).** The whole ZT access loop was proven in tests but nothing ran; this delivers it. `gateway.ParseCatalog(spec)` parses an env catalog `"name=url,name2=url2"` into a `*Catalog` (a malformed entry or host-less/unparseable URL is an ERROR, never silently skipped — a misconfigured route fails loudly, not leaving a service unreachable); `Catalog.Len()` supports config validation. `cmd/openshield-gateway` gains an ACCESS-MODE branch (`OPENSHIELD_ACCESS_MODE`): reusing the worker pool (D76) and Postgres ledger, it loads the identity-aware DEFAULT-DENY access policy from `OPENSHIELD_ACCESS_POLICY` (`policy.New`; it NEVER falls back to the observe-first default, which is default-ALLOW and would admit everyone), parses `OPENSHIELD_ACCESS_CATALOG`, builds `NewAccessProxy` + a `RiskStore` (`SetRiskStore`, populated by A.5b), and serves over TLS that REQUIRES + verifies a client certificate (`RequireAndVerifyClientCert`; client CA from `OPENSHIELD_ACCESS_CLIENT_CA`; server cert/key from `OPENSHIELD_ACCESS_SERVER_CERT`/`_KEY`) on `OPENSHIELD_ACCESS_LISTEN` — a connection without a CA-issued client cert (D86) never completes the handshake. Config is FAIL-FAST and LOUD: a missing client CA, server cert, policy, or an empty catalog ABORTS startup — the failure mode of a Zero-Trust gate must be "does not start", never "starts and admits everyone" (the deployment analogue of the D87 fail-closed decision). A gateway runs as EITHER egress OR access, not both (different roles, different ports; two instances for both). It still spawns the worker and does not link the parser (D72/D84, guarded). PROVEN: a ParseCatalog unit test (a valid spec resolves both services with Len 2; a no-'=' entry and a host-less URL each error); the binary builds; the deps guard stays green. Guards mutation-tested (skip-bad-entries; accept-host-less-url). NOT in scope (stated): the server->gateway risk-publish channel (A.5b, populates the RiskStore); a bespoke access-mode systemd unit (the D84 gateway unit covers isolation; access-mode env is a drop-in); OIDC (A.2b); the device-posture producer; catalog/policy hot-reload (a SIGHUP follow-up like D79). | firm |
| **D91** | **The server→gateway RISK-PUBLISH channel — peer-UEBA risk now flows to continuous verification, making D89 fire (Phase A.5b).** D89 wired the gateway RiskStore + continuous verification but nothing filled the store. Now `RiskUpdate` (subject, risk_score, computed_at) is published on `natsx.SubjectRisk`: `controlplane.Server.PublishRisk` marshals + publishes it (best-effort, logged-not-fatal — never breaks ingest), called in `observePeer` right after a peer alert is recorded; `gateway.ApplyRiskUpdate` decodes it into the RiskStore (a malformed payload or empty subject is an error, never a silent no-op) and `gateway.SubscribeRisk` subscribes and applies each update. The access-mode binary subscribes when NATS is configured, so continuous verification (D89) decides on REAL peer-UEBA risk. This is the T2 model end to end: the server publishes DATA, the gateway's LOCAL policy decides — the server never commands (D14/D54); a compromised server can publish a wrong value (a lie the bounded policy still evaluates) but cannot command the gate. Transport integrity is the connection-level mTLS the fleet NATS already uses (D55); per-message risk signing (like telemetry, D50) is a stated hardening follow-up. PROVEN: `ApplyRiskUpdate` decodes a marshaled RiskUpdate into the store and rejects a malformed/empty-subject payload; the marshal/unmarshal round-trip is symmetric with `PublishRisk`. Guards mutation-tested (no-Set; accept-empty-subject). This closes the last inert edge of the D54 dead-end — peer-UEBA risk now reaches enforcement live. NOT in scope (stated): per-message risk signing (D55 transport auth for now); subject-space unification (endpoint vs gateway subject, a deployment identity-mapping concern); the device-posture producer; OIDC (A.2b). | firm |
| **D92** | **The DEVICE-POSTURE producer — the tamper-lockout (D85) demonstrated end to end with a real device signal (Phase A.5.3).** D85 established that absent device posture fails CLOSED, but nothing produced posture — it was always absent, so a posture-requiring policy denied everyone. Now `PostureUpdate` (subject, compliant, disk_encrypted, agent_present, os_patch_tier, computed_at) is published on `natsx.SubjectPosture`: `gateway.PostureStore` holds the latest per-subject `core.DevicePosture` (its `Set` forces `HasPosture=true` — an entry means posture WAS computed, distinct from absent); `gateway.ApplyPostureUpdate` decodes a published update into the store (a malformed payload or empty subject is an error, never a silent no-op) and `gateway.SubscribePosture` subscribes and applies each; the `AccessProxy` enriches each request's `DevicePosture` from the store via `SetPostureStore`, and the access-mode binary subscribes when NATS is set. A subject with published posture carries it (`HasPosture=true`); a subject with NONE keeps `HasPosture=false` and a posture-requiring policy denies it — the D85 tamper-lockout: a killed/tampered endpoint reports no posture and is denied at the gateway (which the user does not control). This MIRRORS the risk channel (D91) but fails in the OPPOSITE direction: absent RISK is not high (analytics-quiet, don't deny), while absent POSTURE is untrusted (deny) — both expose their absence (D28), the safe direction differs because they mean different things. Still the T2 model: the endpoint produces posture DATA, the gateway's LOCAL policy decides — no server command (D14). PROVEN with real TLS + client certs: a policy gating on `device_posture.has_posture` DENIES a finance client with no posture published (403, the tamper-lockout), and ALLOWS the SAME client once compliant posture is published; `ApplyPostureUpdate` round-trips a PostureUpdate and rejects a malformed/empty payload. Guards mutation-tested (Set-drops-HasPosture; enrichment-forces-HasPosture-on-absent; empty-subject-accepted). NOT in scope (stated): the endpoint agent's posture-REPORT/attestation side (the agent computes + publishes its own device state — a follow-up, like D91's publish side was scoped; self-reported posture is only as trustworthy as the reporter, and TPM/secure-boot measured attestation is the hardening — the absent-posture fail-closed still catches an endpoint that stops reporting); per-message posture signing (D55 transport for now); subject-space unification; OIDC (A.2b). | firm |
| **D93** | **The OIDC/JWT identity producer — the SECOND identity producer beside the client cert (Phase A.2b).** D86's client-cert producer fills the D85 identity contract for deployments that issue a client cert per user; many environments instead federate HUMAN identity through an SSO/OIDC provider. `identity.OIDCVerifier` (`NewOIDCVerifier(issuer, audience, roleClaim, keys)`, `WithClock`) does OFFLINE JWT validation against a STATIC key set: it checks issuer, audience, expiry/not-before, and signature (RS256 — the OIDC default — and EdDSA, consistent with the fleet's Ed25519 keys D60), then resolves a valid token into the SAME `identity.Identity` as a cert — a one-way pseudonymous subject (D23, the raw `sub` never enters the pipeline) and an authorization role read from a configured claim (in the clear, an authz class). EVERY check is fail-closed: a malformed/expired/not-yet-valid/wrong-issuer/wrong-audience/unknown-kid/missing-role/empty-subject token is an error, never a partial or defaulted identity. The `none` alg (the classic JWT bypass) and symmetric HS* are REJECTED, and the alg must match the configured key's type — closing the algorithm-confusion attack (verifying an RSA/HMAC blob against the wrong key). Static-key validation, not live discovery: the gateway is the master chokepoint (D74), so an outbound JWKS fetch on the auth path is a conscious addition, not a default — the operator configures trusted keys; discovery + rotation is a follow-up. PROVEN with real signed JWTs (EdDSA + RS256): a valid token → pseudonymous subject (no raw-identity leak) + role; tampered (EdDSA and RS256), wrong-issuer, wrong-audience, expired, not-yet-valid, unknown-kid, missing-role, empty-subject, `none`-alg, alg-confusion, and not-a-jwt tokens all rejected; a verifier with empty issuer/audience/role/keys is never constructed. Guards mutation-tested (accept-none; skip-issuer; skip-expiry; ignore-RS256-sig-error; drop-RS256-key-type-check). PRODUCER ONLY — composing a user TOKEN with a device CERT (BeyondCorp's two credentials: user identity from the token, device identity + posture from the cert) is the access-proxy wiring follow-up, an owner design fork, staged after this producer exactly as D86 preceded D87. Completes Phase A's identity-producer story (cert OR token). NOT in scope (stated): the dual-credential access wiring; live OIDC discovery/JWKS rotation; refresh/PKCE flows (a client concern). | firm |
| **D94** | **The two-tier inline prefilter — the synchronous tier that turns containment into PREVENTION (Phase B, closes D49's timing gap).** All enforcement was POST-decision (D49): classify fully → decide → record → contain, after the file was already opened (containment, not prevention D16). True prevention answers the fanotify PERMISSION event with DENY before the open completes, but the process blocks uninterruptibly there (D18) so a full parse cannot run inside the window. The two-tier answer: a CHEAP synchronous tier decides within the budget while FULL classification runs async for audit + containment. `internal/agent/prefilter.PreFilter` implements the existing `watchdog.Evaluator` (D18 already owns budget, self-PID, fail-open — this only DECIDES). It ALWAYS submits the full-file async job (tier 2, via `AsyncSubmitter`) so inline prevention NEVER replaces the durable record/containment (D1/D16), and produces an inline BLOCK ONLY for a high-confidence partial deny (action BLOCK AND confidence ≥ a floor, default 0.9 — `New` refuses ≤0; B3): a low-confidence prefix guess ALLOWS the open and defers to async containment (prevent what you can cheaply PROVE, contain the rest). A `PartialDecider` error fails OPEN (VerdictAllow + the error → the watchdog audits it, D17). The prefilter NEVER parses (D13) — the bounded partial classification runs in the sandboxed worker (D72) behind the `PartialDecider` seam; the async full job runs in the unprivileged engine. PROVEN in plain Go: a high-confidence deny → VerdictBlock and drives the REAL watchdog to a kernel Deny; a low-confidence deny/clean/error → Allow; the async job submitted on EVERY path. Guards mutation-tested (floor-ignored; async-dropped; error-hidden; Block-downgraded). NOT in scope, and EMPIRICALLY re-confirmed this session: the privileged permission-mode syscall (B2) — `fanotify_init(FAN_CLASS_CONTENT)` needs INIT-userns CAP_SYS_ADMIN; a `--privileged --userns=host` rootless podman container was tested and returns EPERM (container-root maps to the unprivileged host user, per D52), so the real FAN_OPEN_PERM path is external-gated to a host with genuine root. Also deferred: the fd-passing plumbing (privileged agent→engine reads fd→worker parses a bounded prefix), the PartialDecider implementation, and a distinct DENY_OPEN verb (BLOCK suffices now). | firm |
| **D95** | **The prefilter's PartialDecider made concrete — tier-1 inline prevention proven with a real worker (Phase B).** D94 shipped the two-tier prefilter's decision logic behind a `PartialDecider` seam but left the implementation out of scope. `prefilter.Decider` (`NewDecider`) provides it: it reads a BOUNDED prefix of the permission event's target (double-bounded — a read `LimitReader` caps bytes read/shipped across the IPC in the permission window, a latency/memory guard, and the worker's `MaxBytes` caps the parse), classifies the prefix IN the sandboxed worker (D72 — content parsed in the worker, never in the decider), runs the SAME OPA policy the async engine runs through an audit-LESS dispatcher (no `OnOutcome` — the synchronous tier writes no ledger; the async engine owns the durable record, D16), and returns the Decision to the prefilter only. It runs in the unprivileged engine, so reading a raw prefix is fine — only the worker parses (D13). PROVEN with a REAL worker binary + real OPA BLOCK-on-CPF policy (no Postgres — the sync tier audits nothing): a bounded prefix with a CPF → parsed in the worker → BLOCK at high confidence, and wired through the PreFilter + the REAL watchdog → an inline kernel DENY (a legitimate open prevented) while the async job is still submitted; a clean file → ALLOW; a CPF placed PAST the prefix → ALLOW (the bound is real, deferred to async — the two-tier trade-off proven, not hidden); an empty path → its own distinct error. Guard mutation-tested (empty-path guard — made observable by asserting its distinct message, since `open("")` would otherwise mask it; the read `LimitReader` removal is NOT detection-observable because the worker `MaxBytes` also bounds the parse, so it is kept honestly as an IPC/memory guard, not claimed as test-caught). This is the whole two-tier mechanism real end to end EXCEPT the FAN_OPEN_PERM syscall the environment cannot grant (B2, external-gated, D52). | firm |
| **D96** | **Secrets/credentials detectors — turning "four regexes" into credible detection (Phase D2).** The classifier detected PII (CPF, card, SSN, email) but was blind to the secrets that dominate real exfiltration: private keys, cloud credentials, tokens. Four STRUCTURAL detectors added to the default classifier (`internal/classify/secrets.go`), each a candidate regex + a REAL validator (the CPF/Luhn discipline, never a bare regex): `privateKey` (a `BEGIN … PRIVATE KEY` PEM framing, and NOT `PUBLIC KEY`, conf 0.98); `awsAccessKey` (AWS's published key-id prefix set AKIA/ASIA/… + 16 base32 chars, conf 0.92); `jwt` (base64url-decode the header and require a JOSE header — a JSON object naming `alg`; signature NOT verified — no key; the goal is to tell a real token from a coincidental three-dotted string; a substring check on the decoded bytes avoids a JSON parser in classify; conf 0.85); `apiToken` (vendor prefixes GitHub `ghp_`/`github_pat_`, Slack `xox[baprs]-`, Google `AIza`, OpenAI/Anthropic `sk-`, Stripe `sk_live_`, + a length floor; conf 0.90). New DetectorType enum values PRIVATE_KEY/AWS_ACCESS_KEY/JWT/API_TOKEN (a closed enum, D-classification: a free-form name could itself leak). HIGH confidence because the artifacts are unambiguous — which makes a secrets hit the ideal inline-BLOCK candidate (D94): a leaked private key is exactly what inline prevention should stop. PROVEN: each fires on a real secret (a PEM block, an AKIA key, a genuinely-minted JWT, GitHub/Slack tokens) at high confidence, while benign look-alikes (a PUBLIC key, an SSH public-key line, a non-JOSE three-part token, a wrong-charset AKIA-shaped word, a truncated `sk-`) read CLEAN — the FP discipline of the quality corpus. Guards mutation-tested (JWT-skips-header-check → non-JOSE misdetected; private-matches-public → PUBLIC block tripped; AWS-charset-relaxed → look-alike tripped). Runs in the sandboxed worker (D35); adds no parser to the privileged agent (D72 — classify gained only `encoding/base64`). NOT in scope (stated): document-structure parsing PDF/DOCX/XLSX in the worker (D1, the next Phase-D increment — the RCE surface the split was built for); health-data/IBAN/passport/keyword detectors; admin-authorable detectors + signed distribution (D3, raises the D14 server-push tension); ML/EDM (D4). | firm |
| **D97** | **Document-structure extraction — the classifier reads inside Office files (Phase D1).** The classifier scanned raw bytes, so PII inside an Office document was invisible — a CPF in a .docx is deflate-compressed noise to a byte-level detector. `internal/classify/documents.go` adds `extractOOXML`: it detects a ZIP (the `PK\x03\x04` magic), reads the user-text members (`word/*.xml`, `xl/sharedStrings.xml`, `xl/worksheets/*.xml`, `ppt/slides/*.xml`), strips XML markup with a regex (the detection signal is the text between tags — no heavy XML parser), and returns the concatenated text; `Classify` runs it BEFORE the detectors, so the SAME detectors (PII + the D96 secrets) see the real content. A non-document, a corrupt archive, or one with no recognized text members falls back to a RAW scan — a mis-detection degrades to "scan as-is", never "scan nothing". This is exactly the parser/RCE surface the sandbox split (D29/D35) was built for: it runs in the seccomp worker, never the privileged agent (D72 — classify gained only `archive/zip`). BOUNDED THREE WAYS against a decompression bomb (D13): a per-member `LimitReader`, a 16 MiB total extract budget, and a 4096 entry-count cap — a 4 KB zip declaring gigabytes hits the ceiling and stops. Only the named user-text members are opened, so a nested archive inside the document is never recursively expanded. PROVEN with REAL archive/zip-built documents: a CPF inside a .docx and a token inside an .xlsx are detected (D96 detectors compose over D1 extraction); plain text still works (raw fallback); a non-OOXML zip falls back to a raw scan (a stored CPF is still found); a CPF PAST the per-entry ceiling is NOT extracted (the bound is real and detection-observable); a decompression bomb (a tiny zip declaring 64 MiB) terminates bounded, no OOM. Guards mutation-tested (entry-read-unbounded → a past-ceiling CPF is then found; text-entry-matcher-disabled → document content no longer detected; the zip-magic prefix check removal is NOT behavior-observable — `zip.NewReader` errors on non-zip anyway — so it is kept as a fast-path optimization, not claimed test-caught). NOT in scope (stated): PDF (needs a real parser DEPENDENCY — a separate decision, the next Phase-D candidate); legacy binary Office (.doc/.xls); XML-entity decoding (digits/letters, the detection signal, are not entity-encoded); OCR/image text. | firm |
| **D98** | **International financial + health detectors — completing the Phase-D2 detector set.** The classifier covered Brazilian (CPF) and US (SSN, card) PII plus secrets (D96), but not international financial PII or health data. `internal/classify/international.go` adds two, each with the discipline its evidence warrants. `iban`: an ISO 7064 mod-97-10 checksum (a STRONG validator like CPF, conf 0.93) AND a per-country fixed length — a second, load-bearing guard, because a string can pass mod-97 yet be the wrong length for its country (the test includes exactly such a string, `GB111234567890123456`, and asserts rejection); an unknown country code is rejected outright; the space-grouped printed form is normalized before validation. `healthData`: a small high-signal keyword dictionary with NO checksum, so — like SSN/email — its confidence is capped LOW (0.55) and it requires ≥3 DISTINCT terms to fire (a single common medical word like "diagnosis" is too weak); it is corroborating evidence for a policy, never a strong standalone hit. New DetectorType enum values IBAN/HEALTH_DATA (closed enum). PROVEN: real mod-97-valid IBANs (DE/GB/FR, including the space-grouped form and a letter-bearing BBAN) detected; a wrong-check-digit, a wrong-length (including the mod-97-valid-but-wrong-length string), and an unknown country read CLEAN; multi-term health text fires at low confidence, a single term does not. Guards mutation-tested (mod-97-bypassed → wrong-check-digit accepted; country-length-guard-dropped → mod-97-valid wrong-length GB string accepted; health-threshold-weakened → a single word fires). NOT in scope (stated): passport numbers (country-specific, weak validators); exhaustive IBAN country coverage (a representative SEPA/EU subset — an unknown code is a miss, not a false positive); dictionary tuning/localization against real corpora (T-015) and an admin-authorable dictionary (D3). | firm |
| **D99** | **PDF text extraction — the second document format, with a panic guard (Phase D1).** D97 extracted OOXML; PDF — the other dominant format — was still opaque (its text is compressed inside content streams). `internal/classify/pdf.go` adds `extractPDF`: it detects the `%PDF-` magic, parses via `github.com/ledongthuc/pdf` (a PURE-GO, no-cgo, MIT library — pure Go so it runs behind the worker's seccomp filter, the whole reason the parser surface was split off D29/D35), and extracts plain text bounded by the existing extract ceiling. `Classify` tries it after OOXML; a non-PDF or a parse failure falls back to a raw scan. A PDF parser on hostile input is the canonical crash/DoS surface (the ClamAV PDF CVE that motivated the sandbox), so the parse is wrapped in `recover` — a panic degrades to a raw scan, never crashes the classifier. It does NOT reach the privileged agent (D72; the deps guard stays green — classify is worker-only). PROVEN with a REAL PDF built in-test whose content stream is FlateDecode-COMPRESSED, so the CPF is NOT present verbatim in the raw bytes (the test ASSERTS this) — a detection can therefore only come from real parsing + decompression, not the raw-scan fallback. This caught a genuine bug mid-development: an earlier uncompressed-stream test passed for the WRONG reason (the raw scan saw the plaintext CPF, extraction untested); compressing the stream made the extraction guard observable and a "disable extraction" mutation is now caught. A malformed PDF does not crash and falls back to a raw scan. Guard mutation-tested (extraction-disabled → the compressed CPF becomes unreachable). NOT in scope (stated): encrypted PDFs (a password-callback/credential decision); scanned/image PDFs (OCR); the `%PDF-` magic fast-path and the `recover` guard are defense-in-depth NOT triggered by the current test corpus (every crafted malformed input ERRORED rather than panicked — recover is kept honestly for the adversarial inputs a fuzzer would find). | firm |
| **D100** | **Admin-authorable custom detectors, distributed SIGNED — the D14/T2 tension resolved for detection rules (Phase D3).** Detection was code-only; an operator could not add a custom detector (an internal project code, a customer-ID format) without a release. The hard part is D14: rules must reach the node WITHOUT letting a compromised control plane inject them. Resolved with the T2 model (as risk D91 / posture D92): operator-authored, operator-SIGNED declarative rules the node loads ONLY after verifying the signature. Proto `DetectorRule{rule_id, pattern, confidence, validator}` + `RuleBundle` + `SignedRuleBundle{bundle, signature}` (Ed25519 over the marshaled bundle); `RuleValidator` enum (NONE/LUHN); `DETECTOR_TYPE_CUSTOM`. `internal/classify/rules.go`: `SignRuleBundle` (operator side); `LoadSignedRules` verifies against a trusted operator key BEFORE unmarshaling the inner bundle (an unverified bundle's contents never reach the regex compiler), compiles each rule to a runtime `customDetector`, and is FAIL-CLOSED + ALL-OR-NOTHING (one bad rule fails the whole bundle — a partial load is an ambiguous security state); `Classifier.WithRules` merges built-ins + custom. Rules are DECLARATIVE (a regex + a NAMED built-in validator, never code — so authoring introduces no code path; Go's `regexp` is RE2, linear-time, no ReDoS) and bounded (256 rules, 4 KB pattern). Custom hits report the GENERIC `DETECTOR_TYPE_CUSTOM`, never a per-rule name — a rule named for a customer could itself leak what it detects, so the closed enum's no-leak property (D10/D29) is preserved; rule_id is for bundle management, not surfaced. PROVEN: an operator signs a bundle → the node verifies + loads it → a custom rule fires (as CUSTOM) alongside the still-working built-ins; a LUHN-validated rule uses the built-in validator (a Luhn-invalid number does NOT fire); a wrong-key, tampered, unsigned, or garbage bundle loads NOTHING and errors (the T2/D14 security core); a bad rule (uncompilable regex, out-of-range confidence, unknown validator, over-long pattern) fails the WHOLE bundle. Guards mutation-tested (signature-verify-skipped → forged bundle loads; bad-rule-skipped → partial load; LUHN-replaced-with-true → invalid number fires; the empty-signature guard removal is NOT independently observable — `ed25519.Verify` returns false on an empty sig, so unsigned is still rejected — kept as a clearer error). NOT in scope (stated): per-rule policy routing (a rule id in the classification contract — custom hits currently aggregate under CUSTOM; a contract change is the follow-up); the network distribution CHANNEL (reuse the signed fleet transport like risk/posture — this ships the sign/verify/load core, the wire delivery is next); signed POLICY bundles (the analogous piece for policy vs detectors); a rule-authoring UI. | firm |
| **D101** | **DNS-query connector — network breadth beyond HTTP (Phase C).** The gateway saw HTTP(S), but DNS — a primary egress and exfiltration channel — did not enter the pipeline. `internal/connectors/dns` adds a DNS-query connector so resolution events flow through the SAME pipeline as file and HTTP events. `ParseQuery` is a pure DNS wire-format question decoder (no sockets — the untrusted-bytes surface is tested in ordinary Go, as the fanotify/gateway connectors separate parsing from I/O): it decodes the queried name + qtype, BOUNDED to 255 octets (RFC 1035), and rejects a message that is too short, is a response not a query (QR bit), has a truncated/over-length name, or contains a COMPRESSION POINTER in the question (a QNAME is never compressed, so a pointer is malformed — rejecting it also removes the pointer-loop DoS) — never a partial/empty name as valid (D17). `ToEvent` produces a `EVENT_KIND_DNS_QUERY` NetworkSubject Event (additive enum, the D69 pattern; the queried name in `sni_host` — the flow's destination name, METADATA a policy decides on, never body content D10/D29; udp/53). `TunnelScore` is a DNS-tunneling/exfil heuristic: a covert channel encodes data in long, high-entropy subdomain labels, so the score is the PRODUCT of a length signal and a Shannon-entropy signal — either being low keeps the score low (a long dictionary word is not exfil; a short random token is not a channel); a SIGNAL for a policy, not a verdict. PROVEN with real DNS query bytes: a query parses to name+qtype and produces a DNS_QUERY event; too-short/no-question/response/truncated/compression-pointer messages rejected (including a pointer-with-room message so ONLY the pointer check catches it — the label-bound would otherwise mask it); ordinary names (www.google.com, api.github.com) score low, a base32-encoded exfil label scores high. Guards mutation-tested (label-bound; pointer-in-qname; QR-response-rejection; tunnel product→sum flags normal names). No core interface changed (a new connector + additive enum — the D26/D69 fitness pattern re-validated). NOT in scope (stated): the UDP:53 socket listener / transparent redirect (the I/O half — a live listener + nftables/TPROXY redirect is the external-gated C1 piece, needing root/network-namespace privileges like the fanotify permission mode B2); DNS responses/answer records; EDNS/DNSSEC; an SMTP connector (the next C2 candidate). | firm |
| **D102** | **SMTP-message connector — email breadth (Phase C).** Email is a primary exfiltration channel and did not enter the pipeline. `internal/connectors/smtp` adds an SMTP-message connector so outbound mail flows through the SAME pipeline as file/HTTP/DNS. `ParseSession` is a pure SMTP transcript parser (no sockets — the wire surface tested in plain Go, like DNS/gateway connectors): it extracts the envelope (MAIL FROM / RCPT TO), the DATA message body with DOT-UNSTUFFING (a client line beginning ".." → literal "."; RFC 5321) ending at the lone-"." terminator, is bounded (32 MiB), and rejects a session with no sender, no recipient, or an unterminated DATA block — never a partial message as complete (D17). The message BODY is the classification target — returned for the sandboxed worker (D72), NOT placed in the Event (D10/D29); `ToEvent` produces a `EVENT_KIND_SMTP_MESSAGE` NetworkSubject Event (additive enum) carrying only the recipient DOMAIN in `sni_host` (the destination a policy acts on) — full addresses are PII and are deliberately WITHHELD from the Event even though the worker's email detector may find them in the body. PROVEN with a real SMTP transcript: envelope (from, two recipients, subject) extracted; the body dot-unstuffed (the double dot asserted GONE — a mere `Contains(".line")` would pass either way since it is a substring of "..line", so the test asserts the double-dot's ABSENCE, the recurring isolate-the-guard lesson) and NOT captured past the terminator; the Event carries `partner.example`, never a full address or the body; and the extracted body reaches the classifier — a CPF in the email is DETECTED (email DLP end to end, minus sockets). Malformed sessions (no sender/recipient, unterminated DATA, empty) rejected. Guards mutation-tested (DATA-terminated; no-recipient; dot-unstuffing; domain-not-full-address). No core interface changed (D26/D69). NOT in scope (stated): the port 25/587 socket listener / MTA interception (the I/O side, external-gated like the DNS listener and B2); MIME/attachment decomposition (the body is classified as one blob — the D97/D99 document extractors would compose per-attachment as a follow-up); STARTTLS termination; SMTP responses. | firm |
| **D103** | **Fleet alert search — Phase F (SIEM depth) begins, F1 filtered search over the aggregate.** The operator read API (D82) returned only the N most recent peer alerts — no way to investigate. `Server.SearchPeerAlerts` + `AlertFilter` (subject, min-risk, since, until, limit) filter the fleet-alert aggregate; a `/search` endpoint sits behind the operator-role gate (D58). The WHERE clause is assembled from only the SET constraints, each value appended to the args and bound as `$N` — operator input is DATA, never concatenated into SQL, so the search surface is not an injection vector. PROVEN (Postgres): filtering by subject, by minimum risk (the `>=` comparison verified — no low-risk leaks), by combined subject+risk, and by time window each return the right rows; an injection-shaped subject (`sub_alice'; DROP TABLE peer_alerts; --`) is treated as DATA — it matches nothing and the table is INTACT afterward (a concatenated query would drop it); `/search` is behind the operator gate (an agent role gets 403). Guards mutation-tested (subject-filter-dropped → returns all subjects; min-risk-`>=`-flipped-to-`<=` → returns low-risk rows; since-filter-dropped → returns out-of-window rows). Read-only, forge-nothing (the handler holds no signer, D30); pseudonymous subjects (D23). NOT in scope (stated): full-text search over event PAYLOADS (peer alerts are content-free by design D54/D10/D29 — this searches the fleet-derivation aggregate, not raw evidence, because content stays on the endpoints); cross-host correlation/rules engine (F2); case/investigation workflow (F3); the dashboards/UI (F4, now with a real query API behind it); third-party syslog ingest (F5). | firm |
| **D104** | **Correlation/rules engine — Phase F2, alerts become incidents.** Peer-UEBA (D54) produces individual alerts; a SIEM correlates them into INCIDENTS a human triages. `Server.Correlate` + `CorrelationRule` (window, min-alerts, min-risk) implement the first rule — a BURST: `GROUP BY subject_id HAVING count(*) >= min_alerts` within a time window and above a risk floor, producing `Incident{SubjectID, AlertCount, MaxRisk, FirstSeen, LastSeen}` highest-risk-first; a `/incidents` endpoint sits behind the operator gate (D58). The window cutoff, risk floor, and count threshold are bound as parameters (operator input is DATA). An incident carries enough to triage WITHOUT reading evidence — there is none, peer alerts are content-free and pseudonymous (D23/D54). PROVEN (Postgres): a subject with 4 alerts in the window correlates into ONE incident (count 4, max-risk the 0.99 peak, last-after-first); a single-alert subject and a subject whose alerts fall OUTSIDE the window do NOT; raising the risk floor drops qualifying alerts below the count threshold and the incident disappears; /incidents is behind the operator gate (agent → 403). Guards mutation-tested (HAVING-threshold-weakened-to-1 → every subject becomes an incident; window-cutoff-neutralized → out-of-window alerts count; risk-floor-`>=`-flipped-to-`<=` → wrong alerts selected). NOT in scope (stated): cross-HOST correlation (peer_alerts records subject + time but not the originating host — a host column adds that facet, a follow-up, rather than faked); multi-rule / stateful correlation chains (the Incident type is ready for a general rules engine); case creation from an incident (F3); the UI (F4, now with a real incidents API behind it). Correlates the content-free aggregate (D10/D29), never evidence; parameterized (no SQL injection, as D103). | firm |
| **D105** | **Case/investigation workflow with FOUR-EYES closure — Phase F3.** An incident (F2) needs a workflow to resolve; the security core is four-eyes (D36): closing a case requires two DISTINCT operators, so no single operator can unilaterally close and bury an investigation. Migration `011_cases.sql` (cases + case_notes); `Server.{OpenCase, AssignCase, AddNote, RequestClose, ApproveClose, GetCase}`. Closure is TWO-STEP: `RequestClose` records the requester and sets status `close_requested`; `ApproveClose` closes it — but ONLY when the approver is a DIFFERENT operator than the requester, refusing self-approval with `ErrFourEyes`. Four-eyes is enforced TWICE (defense in depth): an UP-FRONT check (for a clear error) AND the atomic UPDATE predicate `close_requested_by <> $1` (so two operators racing cannot both slip through). Every actor — opened_by/assigned_to/close_requested_by/closed_by — is the operator's VERIFIED certificate identity `operator:<CN>` (D56), never self-asserted; subjects pseudonymous (D23). PROVEN (Postgres): open → assign → note → Alice requests close → Alice CANNOT approve her own closure (ErrFourEyes, the case stays open) → Bob (a different operator) approves and the case closes, recording BOTH parties; approving a closure that was never requested is refused. Four-eyes mutation-tested: enforced by TWO independent guards — removing EITHER alone PRESERVES the control (each guard survives its own removal — honest defense in depth), removing BOTH is CAUGHT (Alice then self-approves and the case closes). The migration-idempotency test's expected count was updated 10→11. NOT in scope (stated): the case HTTP routes (the workflow methods sit behind the operator gate's identity; POST endpoints are a thin follow-up, kept separate so the four-eyes logic is proven independently of transport); incident→case auto-linking (F2 produces incidents, an operator opens the case); the UI (F4); notice/DPIA (T-013 remainder). | firm |
| **D106** | **Syslog ingest connector — third-party log breadth, Phase F5.** OpenShield's telemetry was its own signed fleet stream; F5 adds third-party log ingest so it consumes EXTERNAL sources and becomes a SIEM, not just an audit of itself. `internal/connectors/syslog.Parse` is a pure parser (no sockets — the untrusted-line surface tested in plain Go, like DNS/SMTP): it decodes the priority into facility/severity (PRI = facility*8 + severity, validated 0..191), the timestamp, host, app, and message, across RFC 5424 (auto-detected by the version digit, with STRUCTURED-DATA skipped via a bracket-balanced walk so MSG is the free text not the "[sd...]" prefix) and RFC 3164 (BSD). Bounded (64 KiB/line). The priority is the ONE required field — every syslog message begins with "<PRI>", so a line without a valid priority is REJECTED (it is not syslog; accepting it would pollute the ingest); everything after is best-effort (a truncated header yields what was present, because real-world syslog is famously inconsistent and dropping a whole line over a missing optional field would lose the event) — but a malformed line is an error, never a silently-dropped/mangled record (D17). PROVEN with real syslog lines: an RFC 5424 message decodes to facility/severity 4/2, host, app, message (SD skipped even with internal spaces — the test uses `[exampleSDID@32473 iut="3" ...]`, proving the bracket walk not a naive split); an RFC 3164 BSD line decodes host/tag/message; priority splits exactly across the range (0→0/0, 191→23/7, 86→10/6); malformed lines (empty, no priority, no opening '<', unclosed, non-numeric, out-of-range) rejected. Guards mutation-tested (priority-range → out-of-range accepted; facility/severity-split-swapped → decode wrong; SD-stripping-disabled → message keeps the "[sd]" prefix; missing-'<'-guard → a "34>..." line without the opening bracket accepted, needing an input with a '>' but no '<' to isolate from the '>' check). No core change (a new connector, D26). NOT in scope (stated): the UDP/TCP:514 socket listener (the I/O side, external-gated data-plane as with DNS/SMTP); classifying the message text for PII (the parsed Msg composes with the classifier — a follow-up like SMTP's body); full 5424 structured-data key/value indexing (skipped, not indexed); RFC 5425 (TLS) framing. | firm |
| **D107** | **Incident→case linking — hardening the F2→F3 gap.** F2 produces correlated incidents (D104) and F3 manages cases (D105), but nothing linked them — an operator re-typed the incident context into a new case by hand. `Server.OpenCaseForIncident` opens a case for the incident's subject and writes an opening note summarizing the correlation (alert count, peak risk, time span) in ONE transaction (a case without its originating context is a worse artifact than none), attributed to `system:correlation` (distinct from an operator's own notes — the trail shows machine-derived vs human-added). `Server.CaseNotes` + `CaseNote` read the trail oldest-first. PROVEN (Postgres): an incident opens a case for its subject (opened by the operator, status open) with a SINGLE auto-note carrying the alert count and peak risk, authored by `system:correlation`; a subjectless incident opens no case. Guards mutation-tested (empty-incident-guard → a subjectless incident opens a case; note-drops-count/risk → the summary assertion fails). NOT in scope (stated): case HTTP routes (the methods sit behind the operator gate's identity); AUTOMATIC case creation (an operator triggers it, not a rule — auto-opening per incident would spam; a deliberate follow-up); the UI (F4). | firm |
| **D108** | **Syslog UDP listener — the runnable half, hardening D106.** The syslog connector was a pure parser with no way to receive traffic. `syslog.Listener` (`Listen`, `Serve`, `Addr`, `Dropped`) is the runnable half: it binds a UDP socket, parses each datagram, and delivers the parsed message to a sink. A malformed datagram is DROPPED and COUNTED, never fatal — one bad packet from one noisy source must not stop ingest (an ingest loop that dies on bad input is a DoS waiting to happen, D17); the drop count is exposed (atomic — concurrently read) so a flood of unparseable input is visible, not silent (D28). Shutdown is clean on context-cancel (closing the socket unblocks the read; Serve distinguishes a cancelled ctx → nil from a real error). UDP ingest is testable UNPRIVILEGED (unlike the fanotify permission mode B2), so the running connector is PROVEN with a real socket, not deferred. PROVEN with a REAL UDP socket: two valid datagrams (RFC 3164 + 5424) arrive parsed at the sink; a garbage datagram is dropped, counted, and ingest keeps running; a nil sink is refused; Serve returns nil on clean shutdown. Race-clean (`go test -race`; the drop counter is `atomic.Int64` — fixed a latent race the detector happened not to flag on the first run: `dropped` written in Serve, read in the test). Guards mutation-tested (malformed-delivered-not-dropped → the drop-count assertion fails; nil-sink-guard-removed). NOT in scope (stated): binding the privileged port 514 / transparent redirect (a deployment concern — the address is configurable, runs unprivileged on a high port); TCP syslog (RFC 6587 octet-framing) and TLS (RFC 5425); wiring the sink into the pipeline/classifier (the received records' text composes with the classifier — a follow-up, kept separate so the listener is proven on its own). | firm |
| **D109** | **HIPS exec contract + the T1 action-set expansion — Phase E begins (owner-approved XDR bet).** Phase E makes the endpoint capable of PROCESS CONTROL. Contract-first (as D69 for network): proto `ProcessSubject` (pid, ppid, exec_path, args, parent_path — exec METADATA only, never process memory/content D10/D29; pid is the enforcement target, the process analogue of a file path/flow_id, carried in the Event not the Decision D39), `EVENT_KIND_PROCESS_EXEC`, Event target variant `process`; and the DELIBERATE expansion of the closed action set (T1, D14) with two new typed verbs `ACTION_DENY_EXEC` (refuse an execution — the exec-time analogue of BLOCK) and `ACTION_KILL_PROCESS` (terminate a running process). The verbs were added to EVERY closed-set guard by deliberate edit — the proto AND `validate.go` knownActions AND `policy` actionNames AND the pinned `TestActionEnumIsClosed`/count test — because the whole point of the closed set is that adding a verb is a decision made in code, not a proto value that silently becomes valid; a compromised control plane still cannot express a free-form command, only select one of the finite enforcer-backed verbs. `policy.buildInput` exposes `input.event.{exec_path, parent_path, args}` for a behavioral policy. PROVEN (the D69/D53 fitness pattern): a process-exec Event flows through the UNCHANGED core.Dispatcher → a behavioral policy (a LOLBin `powershell` spawned by an office app `soffice` — suspicious lineage) → a KILL_PROCESS decision → audited via the existing OnOutcome sink → carried out by the EXISTING `TargetedEnforcer` via target = pid. Dispatcher/State/Stage/Registry/Enforcer-interface/OnOutcome/ledger ALL untouched (the third event domain after files and flows, interface unchanged); ProcessSubject carries no memory/content field; both verbs validate under the closed set; the action-count test updated DELIBERATELY (the T1 guard working as designed). NOT in scope (this increment): the behavioral CLASSIFIER (LOLBin/lineage detectors, D110 next); the process ENFORCERS (kill/deny, D111); the exec PRODUCER (auditd/eBPF/fanotify-exec, D112 — the FAN_OPEN_EXEC_PERM permission variant external-gated like B2). Contract + fitness proof only. | firm |
| **D110** | **Behavioral process-detection domain — Phase E, E2 (LOLBin / lineage / encoded-command).** D109's exec contract lets a process event flow the pipeline; this adds what DETECTS abuse — a different classifier shape than content patterns. `internal/behavioral.Analyze(execPath, parentPath, args) Finding` is a pure, deterministic analyzer (like peer-UEBA D53) over exec METADATA only (D10/D29, never process memory): it flags a LOLBin (living-off-the-land binary — powershell/cmd/certutil/rundll32/bash/python/nc/curl…), a suspicious parent→child LINEAGE (an office app or a network server spawning a shell/interpreter — the macro-malware / webshell hallmark), and an ENCODED/download-and-execute command line (`-enc` PowerShell, IEX/DownloadString cradles, curl|bash). `Finding{LOLBin, SuspiciousLineage, EncodedCommand, Score, Reasons}` sums three orthogonal signals (0.35 + 0.4 + 0.35, clamped to 1.0) so the strongest evidence is the CONJUNCTION (office → encoded PowerShell) while a lone signal stays moderate; reasons recorded for the audit trail. `baseName` handles BOTH path separators — a process event may carry a Windows or Unix path, and `path.Base` only splits '/', so a backslash path would otherwise miss every LOLBin. PROVEN: office → encoded PowerShell scores ≥0.9 (the malware hallmark); a webserver → shell (webshell) and a curl-piped-to-bash cradle are flagged; a routine command (git status, an editor launched by the desktop shell) scores 0 (the FP discipline of the content corpus — the suspicious-parent list is deliberately narrow, not "any shell"); the score clamps at 1.0 with 3 reasons; a Windows backslash path resolves to its binary. Guards mutation-tested (LOLBin-disabled; lineage-disabled; encoded-disabled; backslash-path-handling-dropped → the Windows powershell case no longer matches). Evidence for a policy, NOT a verdict — the policy (closed action set D14/D109) decides DENY_EXEC/KILL_PROCESS/ALERT; a high score does not auto-kill. NOT in scope (stated): a behavioral pipeline STAGE exposing the Finding to the policy (the policy already sees the raw exec fields via D109's buildInput — a richer Finding-in-context is a follow-up); an admin-authorable LOLBin/lineage list (like the signed custom rules D100); the enforcers (D111) and producer (D112). | firm |
| **D111** | **HIPS process enforcers — KILL_PROCESS + DENY_EXEC, fail-safe (Phase E, E3).** D109 defined the verbs; this builds the enforcers that carry them out — the process domain implementing the EXISTING `core.TargetedEnforcer` (the third after files and flows, interface unchanged; target = pid / exec handle, Decision carries only the verdict D14/D39). `internal/enforcers/process.KillEnforcer` (KILL_PROCESS) terminates a process by pid and is FAIL-SAFE — like the watchdog fails open, the killer refuses the catastrophic targets: pid ≤ 1 (the kernel and init), its OWN pid, and a non-numeric target, the guards running BEFORE any kill (a process-killer firing on an unparseable or system-critical target is catastrophic). `DenyEnforcer` (DENY_EXEC) records a deny on an `ExecController` seam the permission handler applies (the flow-enforcer pattern D73 — the enforcer never itself answers a kernel exec-permission fd it does not own, so it never races the handler); a deny with no controller or no target is an error, because a deny that goes nowhere silently ALLOWS the execution (D17). Per-OS `platformKill` (syscall.Kill on linux/darwin, a stub elsewhere) keeps the package building on the CI matrix while actuating only where supported (Linux-first D8). PROVEN: KILL_PROCESS terminates a REAL spawned process (rootless — a `sleep 300` is SIGKILLed, confirmed by Wait() returning non-nil, not a clean exit); the enforcer refuses pid ≤ 1, its own pid, and a non-numeric/empty target; DENY_EXEC records the deny on the controller and errors on a nil controller or empty target. Guards mutation-tested (pid≤1; self-pid; nil-controller) — removing the pid≤1 or self-pid guard LITERALLY KILLED THE TEST RUNNER (the mutated code killed pid 1 / the test process), a vivid confirmation the guards matter. NOT in scope (stated): wiring the enforcers into an engine that dispatches process Decisions (the engine registers enforcers per action D49 — a follow-up); the real fanotify FAN_OPEN_EXEC_PERM handler that DENY_EXEC's ExecController fronts (external-gated, needs root like B2); process-TREE kill (only the target pid, not descendants — a follow-up); the pid-reuse TOCTOU (a small window; the exec producer will carry a stabler handle, mirroring the file enforcers' O_NOFOLLOW D65). | firm |
| **D112** | **HIPS exec producer (auditd) — Phase E complete, the process path end to end.** Phase E had the contract (D109), detector (D110), and enforcers (D111), but nothing PRODUCED exec events. `internal/connectors/execaudit` is the observe producer: a pure parser (no I/O — the audit record text tested in plain Go) for Linux auditd EXECVE + SYSCALL records. `ParseSyscall` reads pid/ppid/exe + the audit event id; `ParseExecve` reads the argv; `ToEvent` combines a MATCHED pair (they must share the audit id — stitching an argv onto the wrong process would misattribute the execution, so a mismatch is an error not a guess) into a `EVENT_KIND_PROCESS_EXEC` ProcessSubject Event. Field extraction is WHOLE-TOKEN (the key must start at a boundary) so "pid" is not read from inside "ppid" — a subtle bug the test asserts against directly. Exec metadata only (D10/D29). PROVEN with REAL auditd records: a `powershell -enc` SYSCALL+EXECVE pair parses to pid/ppid/exe + argv and combines into a ProcessSubject Event; the produced event's exec fields feed the behavioral analyzer (D110), which flags it — THE FULL HIPS PRODUCER→DETECTOR PATH end to end; pid is not shadowed by ppid; a record with no exe or no audit id is rejected; a mismatched pair (different audit ids) is not stitched. Guards mutation-tested (whole-token-field → pid reads ppid's value; audit-id-mismatch → unrelated records stitch; no-exe → an exe-less SYSCALL parses). No core change (a new connector, D26). **PHASE E (HIPS) COMPLETE**: D109 contract + T1 action expansion, D110 behavioral detector, D111 kill/deny enforcers, D112 exec producer. NOT in scope (stated): the audit-log tail / netlink audit-socket I/O (privileged — a deployment concern); the fanotify FAN_OPEN_EXEC_PERM PERMISSION producer that enables inline DENY_EXEC before exec (external-gated, needs root like B2 — this is the OBSERVE variant, feeding detection + post-exec KILL_PROCESS); parent_path enrichment (needs a process-tree lookup — left empty, the lineage rules degrade gracefully); hex-encoded argv decoding (a follow-up). | firm |
| **D113** | **SEC-1 (P0): sign & verify the risk and posture channels — the ZT security core was forgeable.** The gateway's risk (D91) and posture (D92) channels decoded published updates with a bare `proto.Unmarshal` and NO signature check, unlike the Ed25519-signed telemetry path. **Anyone able to publish to `openshield.v1.risk`/`openshield.v1.posture` — any enrolled agent, or anyone past broker mTLS — could forge `risk=0` or `Compliant=true` for ANY subject, defeating D89 continuous-verification step-up/deny AND the D85 device-posture tamper-lockout: the security core of the one category (ZT) that actually works.** This was a self-inflicted hole from D91/D92 (the "per-message signing is a follow-up" note I left) — a full audit found it P0. FIX: proto `SignedUpdate{payload, signature}` (Ed25519 over the payload); `gateway.verifySignedUpdate` verifies against a trusted key BEFORE the inner update is parsed (the D100 verify-before-parse pattern); `RiskSubscriber`/`PostureSubscriber` verify each update and reject + COUNT the unverified (`Rejected` counter — no silent drop, D28). Risk is verified against the CONTROL-PLANE key (`controlplane.Server.SetRiskSigner` + `PublishRisk` now SIGNS, and does not publish if unsigned); posture against the publisher key. The gateway binary loads `OPENSHIELD_RISK_PUBKEY`/`OPENSHIELD_POSTURE_PUBKEY` and subscribes to a channel ONLY when its key is configured — it NEVER applies an unsigned update (fail-closed: a forgotten key means an inert channel, not a forgeable one). `openshield-provision risk-keygen` emits the keypair; the server binary loads `OPENSHIELD_RISK_SIGNING_KEY`. PROVEN: a control-plane-signed risk update is applied; a wrong-key (attacker `risk=0`), unsigned (raw RiskUpdate), empty-signature, tampered, or garbage update is REJECTED and the legitimate value STANDS (no forgery overwrites it); the same for posture (a wrong-key `Compliant=true` forgery for a new subject is rejected, the tamper-lockout holds). Guards mutation-tested: **"skip the signature check" FAILS the test** (the SEC-1 core), and the key-length guard prevents an `ed25519.Verify` panic-DoS on a misconfigured key; the negative cases reach real Ed25519 verification, not a routing short-circuit. SCOPE: the RISK half is fully wired + happy-path tested; the POSTURE half verifies now (reject path tested) but its signed PRODUCER is HON-4 — until that lands the channel receives no valid update, and per-subject binding (posture subject == signing agent) is the HON-4 refinement. **Meta-lesson: I built breadth (D92–D112) while a P0 hole I created sat open; the audit backlog's "Bucket S security before any feature" ordering was right.** | firm |
| **D114** | **SEC-2 (P0): enrollment cannot hijack or un-revoke an agent.** `Enroll` used `ON CONFLICT (agent_id) DO UPDATE SET public_key = EXCLUDED.public_key, revoked_at = NULL` — so ANY valid fresh enrollment token could overwrite ANY existing agent's public key (then sign "verified" telemetry as that agent — the replay guard is moot, pick a large sequence) AND un-revoke a revoked agent. The identity foundation the whole verified-telemetry story rests on was overwritable by anyone with a token. FIX: `ON CONFLICT (agent_id) DO NOTHING` + refuse with a new `ErrAgentExists` when `RowsAffected()==0`, WITHOUT consuming the token — race-safe (two concurrent enrolls of the same id: one inserts, the other sees zero rows and is refused). Re-enrollment becomes an explicit, audited operator action (revoke/delete the old identity first), never an implicit upsert a stolen token can trigger. The token is NOT consumed on a rejected hijack, so a legitimate holder can still enroll a fresh id — only the hijack of an EXISTING id is refused. PROVEN (Postgres): a legitimate agent enrolls; an attacker with a DIFFERENT valid token cannot re-enroll that id with their key (ErrAgentExists), and the victim's key still verifies telemetry while the attacker's does NOT; a revoked agent cannot be un-revoked by re-enrollment and stays revoked (its telemetry still returns ErrRevoked). Guards mutation-tested: **restoring `ON CONFLICT DO UPDATE` fails the test** (the attacker's key then verifies as the victim); dropping the existing-agent refusal fails it. NOT in scope (stated): binding a token to a specific agent_id at issuance (a stronger model — a token can only enroll its designated id, a follow-up); an operator key-rotation flow (revoke-then-enroll works today). | firm |
| **D115** | **SEC-3 (P0, + SEC-11): liveness & views count only VERIFIED telemetry over the enrolled roster.** `Overdue`/`LastSeen` aggregated `fleet_telemetry` WITHOUT filtering `verified` — so a dead/compromised agent could be kept "alive" indefinitely by anyone who can publish unsigned telemetry with a self-asserted agent_id (the dead-man's-switch, the mechanism that detects a killed agent, was defeatable by the attacker who killed it), and unverified rows polluted operator views. And `LastSeen` swallowed ALL DB errors as "agent unknown" (SEC-11), so a down database read as agent absence — silently hiding the whole fleet. FIX: `Overdue` now derives liveness from the ROSTER (`agent_identities`, non-revoked) LEFT JOINed to last VERIFIED telemetry — only verified rows count, AND the roster is authoritative so an enrolled-but-silent (or post-purge) agent still surfaces as overdue instead of vanishing (a NULL last-seen → maximally overdue). `LastSeen` filters `verified = true` and scans `max(received_at)` into a `*time.Time` so it distinguishes a query ERROR (returned as an error) from genuine ABSENCE (nil → not-found) — a down DB is a loud failure, not "the whole fleet is unknown" (the error-vs-absence honesty rule D28). Production is unaffected: the fleet-agent already publishes SIGNED heartbeats (SignedPublisher → verified=true), so verified liveness works — this removes the forgeable unsigned path from the authoritative view. PROVEN (Postgres): `LastSeen` finds a verified row but NOT an agent seen only via unverified telemetry, and an unknown agent is absence not error; `Overdue` flags a stale agent, an enrolled-but-never-seen agent (the roster fix), and an agent kept "fresh" ONLY by unverified telemetry (the dead-man's-switch HOLDS), while a verified-fresh agent is not overdue; a closed pool → error, not absence (SEC-11). Guards mutation-tested (LastSeen/Overdue verified-filter dropped → an unverified-only agent is "seen"/not-overdue; LastSeen swallows DB error → a closed pool returns not-found). NOT in scope (stated): removing the unsigned ingest SUBSCRIPTIONS entirely (the unsigned subjects still store verified=false rows now excluded from liveness/authoritative views — full deprecation is a follow-up); `/view` filtering on the same predicate (a small follow-up). | firm |
| **D116** | **SEC-4 (P0): no silent receive-side telemetry loss.** The control plane's NATS subscribers did a synchronous DB insert per message with NO `SetPendingLimits` and NO `ErrorHandler` — a slow consumer overflows the client buffer and drops messages SILENTLY and uncounted, violating the project's own "no silent loss" invariant on the RECEIVE side (the send side has spool + gap detection; the receive side had nothing). FIX: `Server.natsErrorHandler` — an async NATS ErrorHandler installed via `nats.ErrorHandler` at connect — counts (`DroppedMessages` atomic counter) and loudly logs a drop (a SlowConsumer above all); `subscribeCounted` gives every subscription explicit `SetPendingLimits`, so overflow is deterministic and fires the handler rather than relying on the library default. Together the receive side matches the send side's spool/gap discipline: a drop is counted and loud, never a silent vanish. PROVEN (embedded NATS): a flooded slow consumer (a blocking handler + a 1-message pending limit) fires the ErrorHandler — drops are OBSERVED, not a silent zero; and the server's handler increments `DroppedMessages` for every async error. Guard mutation-tested: **"swallow the error handler" (don't count) fails the in-package test**. This OBSERVES loss (the invariant), it does not PREVENT it under sustained overload — NOT in scope (stated): moving ingest to JetStream durable consumers with ack (PLAT-2, which removes the drop window entirely — this is the pending-limits/ErrorHandler stopgap the audit asks for now regardless); agent backpressure signalling; persisting the counter / a metrics endpoint (PLAT-4). | firm |
| **D117** | **HON-3 (P0): wire enforcement into the endpoint engine — production was OBSERVE-ONLY.** The production engine registered ZERO enforcers (`engine.Enforcers` empty; `cmd/openshield-engine` never populated it), so despite the quarantine/encrypt/USB enforcers + the two-tier inline prefilter (D94/D95) + the watchdog (D18) all being built and unit-tested, NOTHING in the running product could contain anything — the README said "observe-only default" but the enforcers were not even REGISTRABLE in the binary. This is the honesty gap the audit named, and the prerequisite that makes DLP containment and Phase B inline prevention MEAN anything. FIX: `cmd/openshield-engine.registerEnforcers` wires the file enforcers into the engine when `OPENSHIELD_ENFORCE` is set (observe-only default preserved, D1, mirroring the gateway's opt-in flow enforcer) — always the quarantine enforcer, and ENCRYPT_LOCAL on top when a symmetric key (`OPENSHIELD_ENCRYPT_KEY`) or a recipient pubkey (`OPENSHIELD_ENCRYPT_PUBKEY`, escrow D59) is configured. Extracted into a testable function so the wiring itself is proven. PROVEN (binary package): with `OPENSHIELD_ENFORCE` a QUARANTINE_LOCAL decision on a CPF-flagged file MOVES it into the quarantine dir and audits an "enforced" outcome; WITHOUT the flag no enforcer is registered and the file is UNTOUCHED (observe-only default holds). Guards mutation-tested: **never-register (the exact HON-3 bug) fails the test** (the file is not quarantined); register-without-the-flag fails the observe-only test. NOT in scope (stated): wiring the USB enforcer (device-attach is a separate producer path); inline prevention timing (Phase B — the prefilter/watchdog are the inline path, external-gated for the syscall; this is post-decision containment D16, not prevention); a config-file surface for enforcement policy (PLAT-5). | firm |
| **D118** | **HON-1 (P1): wire the signed custom rules into the worker — D100 was dead code.** `LoadSignedRules`/`WithRules` — the D100/D3 "admin-authorable signed custom rules", committed as "Phase D complete" — were never called by any binary; the worker built a bare `classify.New()` (built-ins only), so the feature was UNREACHABLE in production (another "committed but unwired" honesty gap, like HON-3). FIX: `cmd/openshield-worker.loadClassifier` merges operator-authored SIGNED custom rules from `OPENSHIELD_RULES_BUNDLE`, verified against `OPENSHIELD_RULES_PUBKEY`, via `WithRules`. FAIL-CLOSED on the rules: a missing key, or an unreadable/unsigned/tampered/wrong-key bundle loads NO custom rules (LoadSignedRules returns nothing on a bad bundle, D100) while the worker still classifies with the built-in detectors — refusing to start would take down classification entirely over a bad OPTIONAL bundle, so availability is preserved and an unverified rule is never trusted (a loud log records the degrade). Opt-in: no bundle path → built-ins only, exactly as before. PROVEN (binary package, real signed bundle on disk): a signed bundle causes a custom detector (`PRJ-\d{6}`) to fire through the worker classifier alongside the built-ins (a CPF still fires); a TAMPERED bundle and a WRONG-KEY bundle load NOTHING (no custom rule fires) while the built-ins still classify. Guard mutation-tested: **never-wire (custom rules not applied) fails the test**; ignoring the worker's error branch SURVIVES because LoadSignedRules is itself fail-closed (D100, proven there) — honest defense in depth. Preserves the no-leak (DETECTOR_TYPE_CUSTOM) and path-vs-content guarantees of D100. NOT in scope (stated): an operator rule-authoring/signing TOOL (a provision subcommand — a small follow-up; today an operator signs via the SignRuleBundle API); hot-reload (SIGHUP, like the CA rotation D79); per-rule policy routing (D100 follow-up). | firm |
| **D119** | **SEC-8 (P2): /search input validation — malformed filters were silently dropped.** The `/search` endpoint (D103) parsed `since`/`until`/`min_risk` with "if it parses, use it; else IGNORE" — a silent drop that turns a typo into an OVER-BROAD result presented as authoritative (an investigator trusts a wrong answer), and `limit` had no upper bound (an unbounded query / memory vector). FIX: `parseAlertFilter` returns an error for ANY malformed param (→ HTTP 400) instead of silently dropping it, and caps `limit` at `maxSearchLimit` (1000); `SearchPeerAlerts` also clamps the limit (defense in depth for a direct non-HTTP caller). An oversized limit is CLAMPED, not rejected — a big ask is honored up to the cap, an unbounded one is not. PROVEN (HTTP, operator-gated): a malformed `min_risk`/`since`/`until`/`limit` → 400; a well-formed search → 200; an oversized `limit` → 200 (capped); the existing D103 injection test stays GREEN (SQL is already parameterized and stays that way). Guards mutation-tested (min_risk/since silently-dropped, limit-not-validated — each then fails to 400). This is the error-vs-silent-default honesty rule (D28) applied to HTTP input. NOT in scope (stated): a configurable cap (hard-coded 1000 — PLAT-5 config surface); pagination cursors (a follow-up when result sets grow). | firm |
| **D120** | **SEC-7 (P2): the prefilter prefix read uses the no-follow safe opener.** The inline prefilter's bounded prefix read (D95) used `os.Open`, not the enforcers' `safeio` TOCTOU discipline (D65) — an attacker who swaps the flagged path for a SYMLINK (or a special file) between classification and the prefilter read could redirect the read onto an arbitrary file. Lower severity (the prefilter is not yet wired to a live permission syscall — Phase B/B2 external-gated), but closed before inline wiring. FIX: refactored `safeio.OpenRegularNoFollow` out of `ReadRegularNoFollow` — the same `O_NOFOLLOW` + fstat-regular-only guarantees, but returning the OPEN FILE so a caller can do a BOUNDED read (the prefilter reads only `maxBytes`, not the whole file, which `ReadRegularNoFollow` would); `ReadRegularNoFollow` now delegates to it (no behaviour change for the enforcers). The prefilter's `openFile` uses `OpenRegularNoFollow`, so the prefix read gets O_NOFOLLOW + regular-file guarantees while staying bounded. PROVEN: the prefilter REFUSES a flagged path swapped for a symlink (mirrors the enforcer safeio tests), while a regular file is still read normally. Guard mutation-tested: reverting to `os.Open` (follows the symlink) fails the test. NOT in scope (stated): the parent-directory-component swap (needs openat2 RESOLVE_NO_SYMLINKS — the documented safeio residual D65); carrying an fd from the fanotify producer through to the read (the strongest TOCTOU fix, tied to Phase B's permission-mode agent, external-gated). | firm |
| **D121** | **SEC-9 (P2): access-proxy header hygiene — a backend could be fed a spoofed identity.** The access reverse proxy (D87/D88) forwarded the original client request WITHOUT stripping client-supplied identity/forwarding headers (`X-Authenticated-User`, `X-Forwarded-*`), and injected NO trustworthy verified-subject header — so a backend behind the gateway could be fed a SPOOFED identity (a client sets `X-Authenticated-User: admin`) and could not consume the real (pseudonymous) one. Hop-by-hop stripping existed on the egress path only. FIX: `sanitizeIdentityHeaders` strips the known spoofable identity/forwarding headers before proxying — INCLUDING `X-OpenShield-Subject` itself, so a client cannot pre-set the trusted header — and the proxy injects the gateway-authoritative `X-OpenShield-Subject` = the verified pseudonymous subject (D23), which `Set` makes authoritative (overwriting any surviving value). The backend gets the same one-way pseudonym the pipeline uses — enough to authorize without leaking the raw identity. PROVEN (real TLS + client cert): a client's spoofed `X-Authenticated-User` and pre-set `X-OpenShield-Subject` do NOT reach the backend; the backend receives `X-OpenShield-Subject` = the verified cert pseudonym (not the spoof); the client's spoofed `X-Forwarded-For` value does not survive the chain (the reverse proxy legitimately sets it to the REAL connecting IP). Guards mutation-tested (don't-strip → the backend receives `X-Authenticated-User=admin`; don't-inject → no/wrong subject; removing the trusted header from the strip list SURVIVES because the inject `Set` overwrites a pre-set value — honest defense in depth). NOT in scope (stated): SIGNING the injected subject header (a backend on the gateway's trust domain trusts the gateway; cross-domain header signing is a follow-up); SrcIP normalization on the access path (host:port vs the egress split — a small cleanup follow-up). | firm |
| **D27** | **`Decision` carries a `context_version`.** Added before any consumer existed, because replay cannot reproduce a Decision without knowing which enrichment context applied, and retrofitting a field into a hash-chained ledger (T-009) means a migration and a break in chain continuity. Empty in Phase 1. | firm |

| **D28** | **Context is a resolved value on `State`, not an accessor, and a closed typed set, not a map.** An accessor would let Policy perform an unbounded lookup inside the permission window D24 protects; an open bag would be a surface through which a compromised control plane could influence decisions by inventing keys — D14's threat by another door. Absent enrichment MUST fail explicitly, never default: a defaulted risk score reads as "safe" and turns an analytics outage into a silent fail-open. Design: [`design-t030-context.md`](design-t030-context.md). Seam implemented, subsystem deliberately not. | firm |

## Security of the platform itself

| # | Decision | Status |
|---|---|---|
| **D13** | **Privilege-split agent; untrusted parsing in an unprivileged sandboxed worker.** Root + `CAP_SYS_ADMIN` + parsing attacker-controlled PDFs is the textbook way to make an org *less* secure — cf. ClamAV **CVE-2025-20260**, a PDF-parser heap overflow to RCE. The privileged process never decodes attacker-controlled bytes. Decompression-bomb limits before any parser runs. | firm |
| **D45** | **The privilege split is enforced by the systemd units, not left to the deployer (T-027).** A boundary that depends on a deployer remembering to drop capabilities erodes to everything-as-root. `deploy/systemd/openshield-agent.service` runs root but bounds capabilities to **only `CAP_SYS_ADMIN`** (fanotify); `openshield-worker.service` runs as a dedicated non-login user with an **empty capability set** plus the seccomp (T-012) and cgroup hardening drop-in (D35); `openshield-server.service` is an ordinary unprivileged service. `deploy/install.sh` is idempotent — binaries, the `openshield`/`openshield-worker` system users, units and drop-ins, `daemon-reload`, enable — refuses to run without root, and does NOT auto-start the agent (fanotify on an unconfigured host is the operator's call). **Upgrade is watchdog-safe (D18):** rebuild → install.sh → `systemctl restart openshield-agent`; a permission event during the restart window fails open (audited) rather than hanging a blocked process, which is why the watchdog was built even though Phase 1 does not enforce. Units are checked with `systemd-analyze verify` (directive-clean). **Not covered:** distro packages / signed artifacts (release engineering), TLS/secrets (deployment), non-systemd init (only Linux+systemd ships, D9); a full install-start cycle needs a systemd host + root and is a documented manual smoke test. | firm |
| **D35** | **The parser worker self-applies a seccomp network-deny filter before reading input (T-012).** Defense in depth on the half of the split that DOES parse attacker bytes (D13/D29): a parser RCE in a worker that cannot call `socket` cannot phone home, exfiltrate over the network, or open a reverse shell. Loaded with `NO_NEW_PRIVS` (no privilege needed) and TSYNC (covers every Go runtime thread; a thread-local filter is trivially bypassable), BEFORE the read loop (a filter applied after the first byte races a fast exploit). A **denylist** (network family + ptrace) in Phase 1, not a strict allowlist: an allowlist breaks whenever the Go runtime uses a new syscall, turning a runtime upgrade into a crash; the intended end state is an allowlist, and this delivers the property that matters now. Off Linux (dev only, D9) `Apply` returns `ErrUnsupported` and the worker warns LOUDLY — an absent sandbox is never a silent pass. Paired with a decompression-bomb guard (ratio/absolute-size/depth, rejected before the bytes reach a parser) and supervisor-enforced cgroup limits (`MemoryMax`, `MemorySwapMax=0`, `CPUQuota`, `TasksMax` in a systemd drop-in — a process cannot self-limit its cgroup without privilege, so that is configuration, not Go theatre). Verified: `socket()` returns EPERM after `Apply()`, including from another thread. | firm |
| **D15** | **Hub uses ed25519 per-author signing**, keys pinned at install, metadata cached with expiry and **fail-closed when stale**. Content-hash-against-index (CrowdSec's model) answers "did I download what the index said", not "who may publish" — and packs execute at high privilege on every endpoint, making this a fleet-wide root-equivalent supply chain. **Offline revocation is fundamentally unsolved**: the propagation window equals the sync interval. Document it; do not imply otherwise. | firm (design deferred) |

| **D29** | **The privilege split is two BINARIES, not one binary with a flag.** A single binary carries the parsers in its dependency graph regardless of which code path runs, which makes the import check — the only mechanism keeping this boundary real — prove nothing. Also stronger than D13 as written: the privileged process never *holds* attacker-controlled content, not merely never parses it. The worker opens files with its own unprivileged credentials and returns detector types, confidences and counts; `LocalClassification`, which holds matched text, never leaves the worker. Enforced by `scripts/check-agent-deps.sh`. | firm |
| **D48** | **The endpoint runs as THREE components — the privileged fanotify agent, the unprivileged network-capable ENGINE, and the sandboxed parser worker — and the three-process shape is forced by the constraints, not chosen (walking skeleton).** Assembling the pipeline surfaced that policy+audit can live in neither the privileged agent nor the worker: OPA uses `encoding/json`, which `check-agent-deps` BANS from the privileged agent (D29), and the audit ledger needs Postgres (network), which the worker's seccomp filter DENIES (D35). So `internal/engine` (unprivileged, network-capable) holds OPA and pgx and runs classify-via-worker → policy → decide → audit with the structured logger (D43 — closing the third deferred seam); the privileged agent holds neither OPA nor a parser (D29 stays enforced); the worker holds parsers but no network (D35 stays enforced). The classify stage bridges the worker's detector hits to `State.Classification` carrying type + confidence + count with EMPTY matched text — no content crosses the IPC (D29) — and a worker error is an auditable failure, never a silent clean result (D17). Proven end to end: a file with a seeded CPF flows through the REAL worker binary → policy → decision → REAL Postgres ledger, landing a verifiable entry. `cmd/openshield-engine` runs it (reloading its signer for write-resume, D46). The fanotify front-end forwarding events to the engine is a thin, privilege-gated step covered by the watchdog (T-011) and spike (T-005); observe-only (D1), it records, it does not enforce. | firm |
| **D52** | **The fanotify observe front-end uses NOTIFY mode with per-directory watches, resolving paths as `watchedDir/name` — which works UNPRIVILEGED; permission mode and FID resolution are the privileged edge, confirmed by probe unavailable in rootless podman.** `internal/connectors/fanotify` watches directories in `FAN_CLASS_NOTIF | FAN_REPORT_DFID_NAME`, decodes each event's mask and filename (a pure `ParseEvent`, unit-tested against the byte layout), and produces `FILE_MODIFIED`/`FILE_CREATED` Events whose path is `filepath.Join(watchDir, name)` — so no privileged `open_by_handle_at` is needed for the per-directory case. It produces paths, never content (D29); classification stays in the worker. **Probed facts, not assumptions:** fanotify PERMISSION mode (blocking) fails without init-namespace CAP_SYS_ADMIN (even `--privileged` rootless podman); `open_by_handle_at` FID resolution fails without CAP_DAC_READ_SEARCH; but NOTIFY-mode init+mark+read+name-extraction works UNPRIVILEGED. Proven end to end HERE, unprivileged: a real file with a seeded CPF written to a watched dir → connector event → engine (real worker + Postgres) → ALERT → verifiable ledger entry — the kernel-event → audit run the walking skeleton fed synthetically. Blocking (permission mode) and recursive/filesystem marks (FID resolution) are the privileged extension, consistent with the deferred inline blocking (T-002). | firm |

| **D30** | **Ledger forward integrity is an evolving Ed25519 KEYPAIR, not a symmetric ratchet — and neither the hash chain nor key evolution suffices alone.** The chain makes an edit detectable but only forces an attacker to rewrite the *tail*; key evolution decides where that rewritable tail *begins*. Together, and only together, they bound tampering to the moment of compromise. The scheme is asymmetric because a symmetric ratchet fails a requirement that is structural, not cryptographic: **verification needs the seed, and the seed forges.** The only party able to verify the log would be the only party able to fake it — which centralises fleet-wide forgery in the control plane, stops an endpoint verifying its own log, and collapses entirely on a single-node air-gapped deployment where no second trust domain exists. Each epoch generates a fresh keypair, publishes its public half signed by its predecessor, and destroys the predecessor's private key; verification walks the public chain from an out-of-band anchor and takes **no secret at all**. **This replaced a shipped implementation that provided no forward integrity whatsoever** — it retained a master seed for the process lifetime and derived every key from it, and the test that "proved" forward integrity passed only because it handed the attacker a derived key rather than the seed the attacker would actually have. Recorded here because the failure was in the threat model, not the code. **Honest limits:** key destruction in Go is best-effort (the GC may retain copies); everything inside the current epoch is forgeable by whoever takes the host; and internal consistency is not completeness — between anchors (T-019) a root attacker can destroy the chain and rebuild a shorter one that verifies perfectly, which is why `Verify` returns a structured result reporting completeness as UNVERIFIED rather than a bare boolean. | firm |
| **D31** | **The agent needs a reachable database to record anything, and there is no offline durability yet.** `Append` fails when Postgres is unreachable, the dispatcher surfaces that error to the caller as `ErrNotRecorded`, and nothing buffers, retries or degrades to a log line. That is deliberate: an entry silently dropped is indistinguishable from an event that never happened, which is the exact failure the ledger exists to prevent, so the gap is made loud rather than papered over. **The operational consequence, stated plainly: a laptop off the network produces no audit trail for that period.** For a product whose honest claim is *detection, not prevention*, this is currently its largest hole — offline store-and-forward is **T-024** and is unresolved until then. Deployments before T-024 lands should treat continuous database reachability as a requirement, not a convenience. | firm |
| **D40** | **The agent store-and-forwards telemetry through a bounded, durable, FIFO disk queue (T-024); overflow drops OLDEST and is a high-severity audit event.** "Offline-capable" is a core principle (D1) and nothing implemented it — a laptop off the network lost every Event it produced. `internal/transport/queue.QueueingTransport` wraps any `core.Transport` (callers unchanged; the seam was shaped for this, D24): reachable + empty queue → publish directly; unreachable OR queue non-empty → persist to disk and return success (the payload is durably held). Each payload is one atomically-written (temp-then-rename) file named by a monotonic sequence, so a crash mid-write cannot corrupt the spool and restart resumes exactly. `Flush` drains strict FIFO and STOPS at the first `ErrUnreachable`, keeping the undelivered tail — an out-of-order or truncated audit trail is a broken one. **The honest guarantee is "no SILENT loss", not "no loss":** the queue is bounded (an unbounded spool is a disk-exhaustion DoS), and on overflow it drops the OLDEST payload and fires a callback the agent wires to a high-severity audit entry (D17 — a silent drop is indistinguishable from nothing happening). Drop-oldest is deliberate: an overflowed evidence queue has already lost its guarantee, and the freshest activity is most likely to matter to an active investigation. **Limits stated:** survives a crash and reboot, not `rm -rf` or a dead disk; strict FIFO, no priority; payloads not encrypted at rest; this is the telemetry path, NOT the audit ledger's own offline durability (still [D31]'s open gap). | firm |
| **D32** | **The public-key chain is part of the ledger, not the signer's memory.** Forward integrity (D30) is only verifiable if the epoch public keys are available to a verifier; until T-010 they lived solely in the in-process `Signer`, so a second process could not verify and a restart with a fresh signer orphaned every prior entry — D30's "verification takes public material only" was true of the algorithm and false of the system. The chain is now persisted in `key_epochs` (public material only; no private column, asserted by test), each epoch written in the same transaction as the first entry that uses it, with a foreign key from `audit_entries.key_epoch` so an entry can never reference an unstored epoch. `Verify` loads the chain from the database and takes a caller-supplied **expected anchor**: given one it fails if the stored chain does not start there; given none it checks internal consistency only and reports completeness UNVERIFIED, naming the absent anchor — the difference between trusting the database to describe itself and not. Surfaced, like the D30 bugs, only when the CLI became the first honest cross-process consumer. **Honest limits:** writing cannot resume with a different signer (that would fork the chain under a new anchor; refused at `Open`), because surviving key material across a restart is T-017; and an anchor exported from a host that could later be compromised only attests to history if it was captured while the host was trusted (T-019 is the real fix). | firm |
| **D46** | **The ledger signer's CURRENT state is exportable/reloadable so an agent resumes its own chain across a restart (T-009 seam).** The public-key chain was persisted (D32) but the current private key lived only in memory, so a restart brought a fresh signer with a new anchor — `postgres.Open` refused it (`ErrCannotResumeWriting`) or a fork resulted. `Signer.Export()`/`core.LoadSigner` serialise the CURRENT epoch's private key, the epoch counter and the public chain; a reloaded signer has the identical anchor, so resume proceeds and appends continue ONE verifiable chain. Only the current epoch's private key is persisted — never a destroyed one — so forward security is intact: the current epoch is already the compromise window, and earlier keys are absent. The blob is integrity-checked (SHA-256 prefix — accidental corruption fails to load, not a MAC) and its private key is validated against the chain's current epoch (a signer never signs under a key the chain omits). `SaveSignerFile` writes it atomically at **mode 0600**; at-rest protection is filesystem permissions + the agent user, defeated by host root exactly as reading process memory is (D16) — encryption at rest (TPM/operator secret) is a noted follow-up that raises the bar against offline disk theft only. The agent's identity key (T-017/D44) is a distinct sibling gap, persisted the same way separately. | firm |
| **—** | **The `openshieldctl` CLI states the trust it does not have.** It is a read surface that holds no signer and can forge nothing (the D30 asymmetry). Until T-017 it authenticates no operator and records no viewer, so it deliberately writes **no** "who viewed this investigation" entry — an unattributable view record would misrepresent the D20 accountability trail as present. It runs for anyone who can reach the database, and it cannot prove completeness without an external anchor. All three limits are printed on the surface, not buried in docs. | firm |

## Project and governance

| # | Decision | Status |
|---|---|---|
| **D21** | **Design for open-core separability now.** Keep managed Hub, compliance packs and multi-tenant control plane cleanly separable. Retrofitting the boundary later is expensive; a CI import test costs one ticket. The sustainability model itself is deferred explicitly. | firm |
| **D37** | **Claim surfaces are guarded against overclaiming by a negation-aware check (T-029), not a naive grep.** The project's credibility rests on "tamper-evident, not tamper-proof" and "detection, not prevention"; a careless README edit could erase it. But a denylist grep is worse than none here — it false-positived on four HONEST negated uses (2026-07-20), because the discipline consists of discussing the forbidden words in negation. `internal/doccheck` scans an explicit allowlist of claim surfaces (README today), flags an UNQUALIFIED overclaim (tamper-proof, unhackable, fully/100% secure, prevents exfiltration, guarantees safety), and suppresses a match that is negated on the line or carries an inline `<!-- allow: term -->` escape; docs/ and research reports are out of scope because they exist to reason out loud. Fixtures prove both directions. Separately, `CheckDecisionRegister` fails on a duplicate D-number — the concrete, robust anti-drift guard that replaces the ticket's original paragraph-length heuristic (itself the same false-positive-prone idea the naive grep failed at). **Honest limit:** the negation heuristic does not understand English; it makes an ACCIDENTAL overclaim near-certain to fail CI, not a deliberate one — a reviewer still reads the copy. | firm |
| **D22** | **Disclose AI authorship.** Owner signs commits and takes responsibility; AI authorship declared via commit trailers and `CONTRIBUTING.md`. Note the open question this raises: AI-generated code may not be copyrightable absent human authorship in several jurisdictions, which would undercut the Apache-2.0 patent-grant rationale. | firm |
| **—** | **Licence: Apache-2.0.** Permissive as the owner requested; chosen over MIT for the explicit patent grant, which matters in a patent-dense field. No field-of-use restriction, so nothing prevents surveillance use — a conscious call, see [`../ETHICS.md`](../ETHICS.md). | firm |

---

## Superseded

- **The brief's Rust agent + Go backend** → D8 (all-Go).
- **"Prefer storing hashes/fingerprints" as a privacy control** → D10/D11. False as written.
- **"Tamper-proof audit logs"** → D12. Unachievable in one self-hosted trust domain.
- **Implied prevention of exfiltration** → D16 and [`threat-model.md`](threat-model.md).
- **`BrowserUploadStarted` in Phase 1** → cut 2026-07-20; needs a browser extension or TLS
  proxy, not a kernel hook. Deferred to Phase 2.
- **The brief's "Zero Trust oriented"** → reframed (D84). The network gateway authenticates no
  subject (the subject is a hashed source IP), so it is content-inspection egress DLP, NOT a
  Zero-Trust enforcement point and NOT a NIPS. Identity-aware authz is on the architecture
  roadmap, not built.

## Known open questions

- **A2 shape questions** — Data Discovery is a catalog and Lineage is a graph; neither has a
  home in the frozen pipeline. T-004 tests only the UEBA sliver of this.
- Whether the fitness test can ever be strong enough: adding an S3 connector is isomorphic to
  what exists and proves little; the paper design of peer-UEBA (T-004) is the real test.
